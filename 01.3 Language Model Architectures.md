# Attention Mechanism & Transformer

## 1. Attention 的核心直觉

Attention 的核心不是公式，而是：

> 当前正在处理一个 token 时，模型去比较“当前关注对象”和一组其他 token，然后判断哪些信息最相关。

可以理解为：

```text
当前 token
   ↓
去看其他 tokens
   ↓
计算相关性
   ↓
给重要 token 更高权重
   ↓
汇总成当前需要的信息
```

例如机器翻译：

```text
French:
L' hippopotame a mangé mes devoirs

English:
The hippopotamus ate my homework
```

当 Decoder 正要生成 `hippopotamus` 时，它应该主要关注 source 中的：

```text
hippopotame
```

这就是 Attention。

---

# 2. Context Vector

在早期 RNN + Attention 模型中，Decoder 在第 $t$ 步会计算一个 **context vector**：

$$
c_t = \sum_{i=1}^{T} \alpha_{t,i} h_i^{enc}
$$

其中：

* $h_i^{enc}$：Encoder 在第 $i$ 个 source token 的 hidden state
* $\alpha_{t,i}$：当前 Decoder step 对这个 source token 的 attention weight
* $c_t$：当前预测所需要的 source information

所以：

> **Context vector = weighted sum of encoder hidden states**

假设：

```text
L'             0.02
hippopotame    0.80
a              0.03
mangé          0.08
mes            0.03
devoirs        0.04
```

那么当前 context vector 主要由 `hippopotame` 对应的 hidden state 决定。

---

# 3. Attention Weight 是怎么计算的？

第一步先算 relevance score：

$$
e_{i,j}
=score(h_i^{enc}, h_{j-1}^{dec})
$$

意思是：

> Encoder 第 $i$ 个 token 和 Decoder 当前状态有多相关？

然后对所有 source positions 做 Softmax：

$$
\alpha_{i,j}
=\frac{\exp(e_{i,j})}
{\sum_{k=1}^{T}\exp(e_{k,j})}
$$

因此：

$$
\sum_i \alpha_{i,j}=1
$$

这些 $\alpha$ 就是 **attention weights**。

---

# 4. Dot-Product Attention

一种最简单的 scoring function：

$$
score(q,k)=q \cdot k
$$

也就是：

**dot product**

直觉：

* dot product 大 → 更匹配
* dot product 小 → 相关性低

后来 Transformer 使用的是：

**Scaled Dot-Product Attention**

---

# 5. Q, K, V 是什么？

Attention 最重要的三个概念：

* **Query (Q)**：我现在想找什么？
* **Key (K)**：每个 token 用什么信息来和 Query 匹配？
* **Value (V)**：如果这个 token 被认为重要，我真正要拿走什么信息？

非常像 database lookup：

```text
Query
  ↓
compare with Keys
  ↓
similarity scores
  ↓
Softmax
  ↓
weights
  ↓
weighted sum of Values
```

面试最容易记的一句话：

> **Query asks, Key matches, Value provides the information.**

---

# 6. Scaled Dot-Product Attention

Transformer 中：

$$
score(q_j,k_i)
=\frac{q_j \cdot k_i}
{\sqrt{d_k}}
$$

其中：

* $q_j$：当前位置 Query
* $k_i$：第 $i$ 个 Key
* $d_k$：Key dimension

然后：

$$
\alpha_{j,i}
=softmax
\left(
\frac{q_j \cdot k_i}{\sqrt{d_k}}
\right)
$$

最后：

$$
o_j
=\sum_i \alpha_{j,i} v_i
$$

得到当前位置的 Attention output。

---

# 7. 为什么除以 $\sqrt{d_k}$？

如果 vector dimension 很大，dot product 的数值也容易很大：

$$
q \cdot k
$$

这样 Softmax 会变得太尖锐：

```text
[20, 1, -5]
→
softmax ≈ [1, 0, 0]
```

会导致梯度变小。

所以除以：

$$
\sqrt{d_k}
$$

来稳定数值。

---

# 8. Matrix Form

如果一次处理整个 sequence，可以把 Q、K、V 写成矩阵。

$$
Q \in \mathbb{R}^{T_q \times d_k}
$$

$$
K \in \mathbb{R}^{T_k \times d_k}
$$

$$
V \in \mathbb{R}^{T_k \times d_v}
$$

最重要公式：

$$
Attention(Q,K,V)
=softmax
\left(
\frac{QK^T}{\sqrt{d_k}}
\right)V
$$

这是 Transformer 最核心公式之一。

---

# 9. 公式一步一步理解

## Step 1：Compute Similarity

$$
QK^T
$$

得到一个：

$$
T_q \times T_k
$$

矩阵。

每个元素表示：

> 一个 Query 和一个 Key 的相关程度。

---

## Step 2：Scale

$$
\frac{QK^T}{\sqrt{d_k}}
$$

避免数值过大。

---

## Step 3：Softmax

$$
A
=softmax
\left(
\frac{QK^T}{\sqrt{d_k}}
\right)
$$

得到：

**Attention Matrix**

每一行就是某个 Query 对所有 Keys 的 attention distribution。

---

## Step 4：Weighted Sum of Values

$$
O=AV
$$

即：

> 根据 attention weights，把重要的 Values 加权汇总。

---

# 10. 为什么从 RNN 转向 Transformer？

课件指出 RNN 有两个重要问题。

## Problem 1：Sequential Computation

RNN：

$$
h_t=f(h_{t-1},x_t)
$$

所以：

```text
h1 → h2 → h3 → h4
```

必须一个接一个计算。

因此：

> **cannot be fully parallelized**

训练速度慢。

---

## Problem 2：Long-Range Dependency

如果两个 token 相隔 $K$ 个位置：

```text
token1 → ... → tokenK
```

信息要经过很多 hidden states 才能传过去。

中间每一步都有可能丢失信息。

所以 RNN 对 long context 不够友好。

---

# 11. 为什么 Attention 可以解决？

Self-Attention 可以让：

```text
token 1 ─────────────→ token 100
```

直接建立联系。

不需要经过：

```text
token2 → token3 → ... → token99
```

因此更容易捕获：

**long-range dependency**

另外 training 时各个 token position 的 attention 可以并行计算。

---

# 12. 2017：Attention Is All You Need

2017 年 Transformer 论文的核心创新：

> 不再依赖 RNN recurrence，而只使用 Attention + Feed Forward Network。

从：

```text
RNN + Attention
```

变成：

```text
Attention + Feed Forward
```

这就是：

**Transformer**

---

# 13. Self-Attention

之前 Encoder-Decoder Attention 是：

> 一个 sequence 去关注另一个 sequence。

而 **Self-Attention** 是：

> 一个 sequence 关注它自己。

例如：

```text
The animal didn't cross the street because it was tired.
```

模型处理 `it` 时，可以直接关注：

```text
animal
```

从而建立 token 之间的关系。

---

# 14. Self-Attention 中 Q/K/V

对于同一个 input sequence $X$：

$$
Q = XW_Q
$$

$$
K = XW_K
$$

$$
V = XW_V
$$

也就是说：

> Q、K、V 都来自同一个 sequence，只是经过不同 learned projections。

所以叫：

**Self-Attention**

---

# 15. Cross-Attention / Encoder-Decoder Attention

在 Encoder-Decoder Transformer 中：

Decoder 需要读取 Encoder 的结果。

这时：

$$
Q = H^{dec}
$$

$$
K = H^{enc}
$$

$$
V = H^{enc}
$$

即：

* Query 来自 Decoder
* Key / Value 来自 Encoder

所以叫：

**Cross-Attention**

---

# 16. Self-Attention vs Cross-Attention

| Type                | Query         | Key           | Value         |
| ------------------- | ------------- | ------------- | ------------- |
| **Self-Attention**  | same sequence | same sequence | same sequence |
| **Cross-Attention** | Decoder       | Encoder       | Encoder       |

一句话：

> Self-attention models relationships inside one sequence; cross-attention lets one sequence retrieve information from another.

---

# 17. Transformer 为什么需要 Positional Encoding？

RNN 本身有顺序：

```text
x1 → x2 → x3
```

但 Attention 一次看所有 token，本身不知道顺序。

所以：

```text
dog bites man
```

和：

```text
man bites dog
```

如果只有 token embeddings，模型没有明确 position 信息。

因此 Transformer 输入通常是：

$$
H_0
=TokenEmbedding
+
PositionalEncoding
$$

---

# 18. Transformer Encoder

经典 Encoder layer：

```text
Input
 ↓
Multi-Head Self-Attention
 ↓
Add & Norm
 ↓
Feed Forward
 ↓
Add & Norm
```

重复 $N$ 层。

---

# 19. Add & Norm

课件中的：

```text
Add & Norm
```

其实包含两件事。

## Residual Connection

$$
x + Sublayer(x)
$$

让原始信息直接绕过 sublayer。

作用：

* 改善 gradient flow
* 深层网络更容易训练

## Layer Normalization

$$
LayerNorm(x + Sublayer(x))
$$

帮助稳定训练。

---

# 20. Feed Forward Network

每个 token 都独立通过相同的 FFN：

$$
FFN(x)
=\max(0,xW_1+b_1)W_2+b_2
$$

原始 Transformer 用 ReLU。

直觉上：

* **Attention**：让 tokens 之间交流
* **FFN**：对每个 token 自己做 nonlinear transformation

这个区别很重要。

---

# 21. Transformer Decoder

经典 Decoder layer：

```text
Masked Multi-Head Self-Attention
 ↓
Add & Norm
 ↓
Encoder-Decoder Attention
 ↓
Add & Norm
 ↓
Feed Forward
 ↓
Add & Norm
```

---

# 22. Masked Self-Attention

Decoder 是 autoregressive 的：

$$
P(y_t \mid y_{1:t-1})
$$

预测第 $t$ 个 token 时不能看到 future tokens。

所以需要：

**Causal Mask**

例如：

```text
Token 1 → can see 1
Token 2 → can see 1,2
Token 3 → can see 1,2,3
Token 4 → can see 1,2,3,4
```

Attention Matrix：

```text
✓ ✗ ✗ ✗
✓ ✓ ✗ ✗
✓ ✓ ✓ ✗
✓ ✓ ✓ ✓
```

未来 position 被 mask。

---

# 23. Mask 是怎么实现的？

未来 token 的 attention score 被设成：

$$
-\infty
$$

然后经过 Softmax：

$$
exp(-\infty)=0
$$

所以 future token 的 attention weight：

$$
0
$$

这样模型就无法偷看答案。

---

# 24. Shifted Right

Decoder training 时 target input 要 **shift right**。

例如真实 target：

```text
I love NLP <EOS>
```

Decoder 输入：

```text
<BOS> I love NLP
```

训练目标：

```text
I love NLP <EOS>
```

对应：

```text
<BOS> → I
I     → love
love  → NLP
NLP   → <EOS>
```

这样每一步只利用 previous tokens 预测 next token。

---

# 25. Multi-Head Attention

Transformer 不只计算一个 attention。

而是：

> 同时计算多个 Attention Heads。

假设有：

$$
h=8
$$

则模型有 8 个不同的 Q/K/V projection spaces。

第 $i$ 个 head：

$$
head_i
=Attention
\left(
QW_Q^{(i)},
KW_K^{(i)},
VW_V^{(i)}
\right)
$$

最后：

$$
MultiHead(Q,K,V)
=Concat(head_1,\dots,head_h)W_O
$$

---

# 26. 为什么需要 Multi-Head？

一个 attention head 只是在一个 representation space 中看关系。

多个 heads 可以同时学习不同关系，例如：

* local dependency
* syntax
* subject-verb relation
* coreference
* semantic relation

但面试时不要说：

> 某个 head 一定对应 grammar。

更准确是：

> Different heads can learn different patterns or representation subspaces.

---

# 27. Transformer 中的三种 Attention

## Encoder Self-Attention

所有 source tokens 可以互相看。

```text
Q = Encoder
K = Encoder
V = Encoder
```

通常是：

**Bidirectional / Non-causal**

---

## Decoder Masked Self-Attention

Decoder token 只能看 current + previous tokens。

```text
Q = Decoder
K = Decoder
V = Decoder
```

但有：

**Causal Mask**

---

## Encoder-Decoder Attention

Decoder 去读取 Encoder 信息：

```text
Q = Decoder
K = Encoder
V = Encoder
```

也叫：

**Cross-Attention**

---

# 28. 三种 Transformer Architecture

## Encoder-Only

例如：

**BERT**

使用：

**Bidirectional Self-Attention**

适合：

* classification
* understanding
* representation learning

---

## Decoder-Only

例如：

**GPT**

使用：

**Causal Self-Attention**

适合：

* next-token prediction
* text generation

---

## Encoder-Decoder

例如：

**T5**

Encoder：

```text
Bidirectional Self-Attention
```

Decoder：

```text
Causal Self-Attention
+
Cross-Attention
```

适合：

* translation
* summarization
* Seq2Seq

---

# 29. Transformer 最后如何得到 Next Token？

经过多层 Transformer：

$$
h_t
$$

得到当前 position 的 contextual hidden representation。

然后 Linear：

$$
z_t = W h_t+b
$$

得到：

**logits**

维度：

$$
|V|
$$

即 vocabulary size。

然后：

$$
P(Y_t=i)
=\frac{\exp(z_{t,i})}
{\sum_j \exp(z_{t,j})}
$$

得到 next-token probability distribution。

所以最终：

```text
Tokens
 ↓
Embedding + Position
 ↓
Transformer Layers
 ↓
Hidden Representation
 ↓
Linear
 ↓
Logits
 ↓
Softmax
 ↓
Next-token Probability
```

---

# 30. Decoder Step-by-Step

课件最后一张 Decoder 图可以简化成：

```text
Decoder Input
 ↓
Masked Self-Attention
 ↓
Residual + LayerNorm
 ↓
Cross-Attention to Encoder
 ↓
Residual + LayerNorm
 ↓
Feed Forward
 ↓
Residual + LayerNorm
 ↓
Next Layer
```

如果是 Decoder-only GPT，则没有 Cross-Attention：

```text
Masked Self-Attention
 ↓
Feed Forward
```

重复多层。

---

# 31. 为什么 Transformer 比 RNN 更强？

最重要三个原因：

### 1. Parallelism

RNN：

```text
t1 → t2 → t3 → t4
```

Transformer training：

```text
t1
t2
t3
t4
```

可以一起处理。

---

### 2. Long-Range Dependency

RNN 两个远距离 token 的信息要经过很多 steps。

Self-Attention：

> 任意两个 token 在一层里就可以直接交互。

---

### 3. Better Scaling

Transformer 的计算非常适合：

* GPU
* TPU
* large matrix multiplication

因此更容易扩展到：

* more data
* more parameters
* larger models

---

# 32. Transformer 的一个重要缺点

标准 Self-Attention 要计算：

$$
T \times T
$$

attention matrix。

所以 sequence length 为 $T$ 时：

$$
Time/Memory \approx O(T^2)
$$

context 越长越贵。

这也是后来很多优化的来源：

* FlashAttention
* Sparse Attention
* Sliding Window Attention
* Long-context architectures

---

# 33. 面试：Explain Attention

可以这样回答：

> Attention compares a query with a set of keys to compute relevance scores. After normalizing the scores with softmax, the model uses them as weights to compute a weighted sum of the values.

中文理解：

> Query 和所有 Keys 做匹配，Softmax 得到 attention weights，然后对 Values 做 weighted sum。

---

# 34. 面试：What are Q, K, V?

> Query represents what the current token is looking for. Keys represent what each token can be matched against, and values contain the information that is aggregated if that token receives high attention.

中文：

> **Q = 我要找什么，K = 我怎么和别人匹配，V = 匹配到以后拿什么信息。**

---

# 35. 面试：Why Self-Attention instead of RNN?

> RNNs process tokens sequentially, which limits parallelism and makes long-range dependencies difficult to preserve. Self-attention allows every token to directly interact with other tokens and enables parallel computation during training.

---

# 36. 面试：Self-Attention vs Cross-Attention

> In self-attention, Q, K, and V come from the same sequence. In cross-attention, Q comes from one sequence, typically the decoder, while K and V come from another sequence, typically the encoder.

---

# 37. 面试：Why Causal Mask?

> A causal mask prevents a token from attending to future tokens, preserving the autoregressive property required for next-token prediction.

---

# 38. 最重要的公式

一定要熟悉：

$$
Attention(Q,K,V)
=softmax
\left(
\frac{QK^T}{\sqrt{d_k}}
\right)V
$$

把它读成：

```text
Q × K^T
   ↓
Similarity Scores
   ↓
Scale
   ↓
Softmax
   ↓
Attention Weights
   ↓
× V
   ↓
Weighted Information
```

---

# 39. 一张图记住整个 Transformer

```text
Tokens
  ↓
Token Embeddings
  +
Positional Encoding
  ↓
Self-Attention
  ↓
Add & Norm
  ↓
Feed Forward
  ↓
Add & Norm
  ↓
Repeat N Layers
  ↓
Hidden States
  ↓
Linear
  ↓
Logits
  ↓
Softmax
  ↓
Token Probabilities
```

对于 Encoder-Decoder：

```text
Source
 ↓
Encoder Self-Attention
 ↓
Encoder Hidden States
          ↓
      Cross-Attention
          ↑
Target → Masked Self-Attention
          ↓
       Decoder
          ↓
        Logits
          ↓
       Softmax
```

---

# 40. 最终一句话总结

> **Attention lets a token dynamically retrieve relevant information from other tokens. Transformers replace recurrence with self-attention, enabling parallel training and direct long-range interactions. Self-attention uses Q, K, and V from the same sequence, while cross-attention uses decoder queries to retrieve information from encoder keys and values.**
