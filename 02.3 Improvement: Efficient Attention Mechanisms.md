
# Efficient Attention Mechanisms：大白话理解 MHA、GQA、MQA、MLA 与 KV Cache

<img width="777" height="515" alt="image" src="https://github.com/user-attachments/assets/0a1ff216-8ec3-46cf-bfae-1a04eaa3955e" />


## 0. 这一节到底在解决什么问题？

先只记住一句话： **现代 Efficient Attention 的一个重要目标，是让模型生成文字时，不要为了保存历史 token 的 K 和 V 占掉太多 GPU 显存。**


<img width="984" height="488" alt="image" src="https://github.com/user-attachments/assets/a475cc50-e504-4d95-8634-dee1169c69fb" />


你前面已经学过标准 Attention：

$$
Attention(Q,K,V) =
softmax\left(\frac{QK^T}{\sqrt{d_k}}\right)V
$$

可以继续用之前的心智模型：

```text
Q = 我现在想找什么？
K = 我以前保存的“索引/标签”
V = 这个索引对应的实际信息
```

这几张 slide 的重点并不是推翻 Attention，而是在问： **K、V 能不能少存一点？**

演进可以先记成：

```text
MHA
每个 Query head 都有自己的 K/V
        ↓
        很占 KV Cache

GQA
几个 Query heads 共用一套 K/V
        ↓
        更省

MQA
所有 Query heads 共用一套 K/V
        ↓
        更省

MLA (最efficient的)
不直接缓存完整 K/V
而是缓存一个压缩后的 latent representation
        ↓
        进一步压缩 KV Cache

Multi-head latent attention achieves the greatest reduction in KV cache by projecting the K and V matrices to a lower latent dimension using a weight matrix.

```
<img width="949" height="501" alt="image" src="https://github.com/user-attachments/assets/99d548c5-18c1-4653-b9ea-2420d171d999" />


---

# 1. 为什么突然这么在乎 K 和 V？

## Training 和 Generation 有一个重要区别

训练时，我们通常一次处理很多 token。但 autoregressive generation 是：

```text
Prompt:
I love

生成：
cats

然后：
I love cats

再生成：
because

然后：
I love cats because

...
```

每次只生成一个新的 token。

假设已经有：

```text
I love cats very much
```

现在生成下一个 token。

Attention 需要让新 token 的 Query 去查看之前所有 token 的 Key 和 Value。

如果每次都重新计算历史 token 的 K/V：

```text
I     → 重新算 K,V
love  → 重新算 K,V
cats  → 重新算 K,V
very  → 重新算 K,V
much  → 重新算 K,V
```

非常浪费。

于是实际 inference 通常会： **把之前 token 的 K 和 V 保存下来。**

这就是：KV Cache

---

# 2. KV Cache 用大白话理解

想象你在写论文。

每读完一本书，你都做：

```text
索引卡 = K
笔记内容 = V
```

以后需要资料时：

```text
Q = 我现在的问题

Q 和 K 比较
↓
找到相关笔记
↓
读取 V
```

你不会每次提一个新问题，都重新把所有书读一遍。

你会把以前的：

```text
K + V
```

留着。

这就是 KV Cache 的意义： **用显存换计算时间。**

---

# 3. 为什么 KV Cache 会越来越大？

假设：

```text
Sequence length = S
Layers = L
Heads = H
Head dimension = HD
```

每一层、每个历史 token 都需要保存 K/V。

标准 MHA 中，每个 head 都有自己的 K 和 V。

粗略看，KV cache 会随着：

```text
sequence length ↑
number of layers ↑
number of KV heads ↑
head dimension ↑
batch size ↑
```

一起增长。所以长 context会让 KV Cache 成为 inference 的重要显存和 memory-bandwidth 成本。

---

# 4. 标准 Multi-Head Attention（MHA）

你之前学的标准 Multi-Head Attention 可以想成有 8 个“研究员”。

```text
Q1 → K1, V1
Q2 → K2, V2
Q3 → K3, V3
Q4 → K4, V4
Q5 → K5, V5
Q6 → K6, V6
Q7 → K7, V7
Q8 → K8, V8
```

每个 Query head：**都有自己专属的一套 K 和 V。**

如果：H = 8 heads

那么：

```text
8 Query heads
8 Key heads
8 Value heads
```

图：

```text
Q1 ── K1,V1
Q2 ── K2,V2
Q3 ── K3,V3
Q4 ── K4,V4
Q5 ── K5,V5
Q6 ── K6,V6
Q7 ── K7,V7
Q8 ── K8,V8
```

优点：每个 head 有最大的独立性

问题：

```text
每个历史 token
×
每一层
×
8 套 K/V

都需要 cache
```

所以 inference 时很占显存。

---

# 5. 为什么主要压缩 K/V，而不是 Q？

这是一个非常重要的问题。生成第 1001 个 token 时：Q主要是当前新 token 的 Query。

算完这一步以后，就不需要把这个 Q 为未来一直保存下来。

但是历史 token 的：K，V， 未来第 1002、1003、1004... 个 token 都还要继续查询。

所以：Q 临时使用，K/V历史资料，需要长期缓存

因此 efficient attention 很多设计都把重点放在： **减少需要保存的 K/V。**

---

# 6. Grouped-Query Attention（GQA）

现在假设还是：8 Query heads

MHA 是：

```text
8 Q
8 K
8 V
```

GQA 提出： **没必要每个 Query head 都拥有自己的 K/V，可以几个 Query heads 共享一套 K/V。**

例如分成 4 组：

```text
Q1 ─┐
    ├── K1,V1
Q2 ─┘

Q3 ─┐
    ├── K2,V2
Q4 ─┘

Q5 ─┐
    ├── K3,V3
Q6 ─┘

Q7 ─┐
    ├── K4,V4
Q8 ─┘
```

于是：

```text
Query heads = 8

KV heads = 4
```

KV Cache 大约只需要原来一半的 KV-head storage。

---

# 7. 用“公司资料库”理解 GQA

MHA：

```text
8 个员工

每个人都有：
自己的档案柜 K
自己的资料库 V
```

就是：

```text
员工1 → 柜子1
员工2 → 柜子2
...
员工8 → 柜子8
```

很灵活，但是要维护 8 套资料。

GQA：

```text
员工1、2 → 共用资料柜 A
员工3、4 → 共用资料柜 B
员工5、6 → 共用资料柜 C
员工7、8 → 共用资料柜 D
```

员工仍然有自己的：Q = 自己的问题 / 查询方式


但几个人共享：K/V = 历史资料

于是： **保留多个 Query heads 的多样性，同时减少重复的 K/V storage。**

---

# 8. GQA 为什么是一个 compromise？

两个极端：

```text
MHA
每个 Q 都有自己的 K/V
→ 灵活
→ 贵
```

另一个极端：

```text
所有 Q 都共用同一套 K/V
→ 最省
→ K/V 多样性最低
```

GQA 放中间：

```text
几个 Q 一组
共享一套 K/V
```

所以它是在平衡：

```text
Model quality
        ↕
Inference efficiency
```

---

# 9. Multi-Query Attention（MQA）

如果把 GQA 推到极端： **所有 Query heads 共用一套 K 和 V。**

例如：

```text
Q1 ─┐
Q2 ─┤
Q3 ─┤
Q4 ─┤
Q5 ─┼──→ K1,V1
Q6 ─┤
Q7 ─┤
Q8 ─┘
```

所以：

```text
8 Query heads

但是只有：

1 Key head
1 Value head
```

这就是：Multi-Query Attention（MQA）

---

# 10. MHA、GQA、MQA 一眼看懂

假设有 8 个 Query heads：

## MHA

```text
8 Q
8 K
8 V
```

```text
Q1 → K1,V1
Q2 → K2,V2
...
Q8 → K8,V8
```

---

## GQA（4 KV groups）

```text
8 Q
4 K
4 V
```

```text
Q1,Q2 → K1,V1
Q3,Q4 → K2,V2
Q5,Q6 → K3,V3
Q7,Q8 → K4,V4
```

---

## MQA

```text
8 Q
1 K
1 V
```

```text
Q1 ─┐
Q2 ─┤
... ├→ K1,V1
Q8 ─┘
```

所以：

```text
MHA ─────────→ GQA ─────────→ MQA

KV heads:
8              4              1

KV Cache:
大              中              小
```

---

# 11. 为什么不全部直接用 MQA？

因为： **压缩越激进，越可能损失一些 representation capacity / quality。**

MHA：

```text
每个 head：
自己的 Q
自己的 K
自己的 V
```

自由度最大。

MQA：

```text
不同 Query heads
必须共享同一个 K/V representation
```

节省很多 memory，但可能限制表达能力。

GQA 因此是一个很实用的中间方案：

```text
比 MHA 省很多

但比 MQA 保留更多 KV diversity
```

课程图中的实验也是在表达这种 quality / speed tradeoff。

---

# 12. Slide 中 GQA 的实验图怎么看？

左边：Performance，MHA 是 reference。

把已有 MHA 转成 GQA 后，如果进行少量：uptraining

GQA 的 performance 可以恢复得很接近原来的 MHA。

右边：Time per sample

KV groups 越少：

```text
需要读取的 K/V 越少
→ generation 更快
```

当 groups 越来越接近所有 heads：GQA → MHA，速度优势逐渐消失。

所以这张图想告诉你：**GQA 可以在接近 MHA quality 的情况下，明显减少 generation cost。**

---

# 13. 什么叫把已有 MHA 模型转换成 GQA？

不一定要从头训练一个 GQA 模型。

假设已有 MHA：

```text
K1
K2
K3
K4
...
K8
```

如果想变成 4 个 KV groups，可以把同组的 projection 做 mean pooling。

例如：

```text
K1
K2
 ↓
average
 ↓
K_group1
```

类似地处理 V。

于是：

```text
MHA
↓
pool / compress KV heads
↓
GQA
↓
再进行少量训练适应
```

课程 slide 称这种继续训练为：uptraining

所以 efficient attention 可以：

```text
方法 1：
直接用新 architecture pretrain

方法 2：
把已有 MHA 模型压缩/转换后继续训练
```

---

# 14. Multi-Head Latent Attention（MLA）又在干什么？

前面的方法都在问：到底保存多少套 K/V？


MHA：很多套

GQA：少几套

MQA：只留一套

MLA 换了一个思路： **为什么一定要直接保存完整的 K 和 V？能不能先把它们压缩成一个更小的 latent vector？**

---

# 15. 用“压缩文件”理解 MLA

原来每个 token 需要保存：K + V

想象一共：100 MB

MLA 的思路类似：

```text
完整 K/V
↓
压缩
↓
一个更小的 latent representation
↓
只把这个压缩版本放进 cache
```

需要 Attention 时，再通过 learned projections 使用/恢复 K、V 所需的信息。

所以：

```text
MHA/GQA/MQA：
主要减少 KV heads 数量

MLA：
把 KV 信息压缩进低维 latent representation
```

---

# 16. MLA 的公式用大白话看

Slide：

$$
c_t^{KV}=W^{DKV}h_t
$$

不要怕。它只是：

```text
当前 hidden state h_t
        ↓
一个 learned down-projection
        ↓
压缩成更小的 c_t^KV
```

这个：

$$
c_t^{KV}
$$

就是： **compressed latent KV representation**

真正 cache 的主要是这个较小的 representation。

---

然后理论上：

$$
k_t=W^{UK}c_t^{KV}
$$

$$
v_t=W^{UV}c_t^{KV}
$$

意思是：

```text
compressed latent
      ↓
projection
      ↓
得到 K 所需表示

compressed latent
      ↓
另一 projection
      ↓
得到 V 所需表示
```

其中：

```text
D = Down projection
U = Up projection
```

---

# 17. 为什么 slide 说 recovery 可以 merge？

你可能会想： “先压缩，再恢复 K/V，不是又多算了一遍吗？”

MLA 的一个关键工程/数学技巧是： 某些恢复 K/V 的线性变换，可以和 Attention 中其他 projection/matrix operations 合并。

所以 inference 时不一定真的显式做：

```text
latent
↓
完整恢复 K
↓
完整恢复 V
↓
再 Attention
```

可以通过矩阵运算重组，把部分操作融合。

对这门课目前最重要的不是推导，而是： **Cache 中不需要保存完整 K/V，只保存压缩 latent，因此大幅减少 KV Cache。**

---

# 18. 四种机制放在一起

假设：8 Query heads

## MHA

```text
Q: 8
K: 8
V: 8

Cache：
████████
████████
```

---

## GQA

```text
Q: 8
K: 4
V: 4

Cache：
████
████
```

---

## MQA

```text
Q: 8
K: 1
V: 1

Cache：
█
█
```

---

## MLA

```text
Q: multiple heads

K/V information
↓
compressed latent

Cache：
▌
```

MLA 不是简单等同于“比 MQA 永远更小”；实际大小取决于具体 dimensions 和 architecture。

真正区别是：

> **MLA 缓存的是压缩 latent representation，而不是按传统方式直接缓存完整 per-head K/V。**

---

# 19. Slide 里的 DeepSeek 图在说什么？

图中比较传统 DeepSeek 67B architecture 与使用 MLA 的 DeepSeek-V2 的 KV cache storage。

课程 slide 标注：

```text
KV cache reduced by 93.3%
```

它想强调的不是让你背具体 KB/token，而是： **通过 latent KV compression，可以大幅降低 generation 时的 KV Cache memory footprint。**

这对于：

```text
long context
large batch inference
high-throughput serving
```

特别重要。

---

# 20. 为什么 KV Cache 小了会让 generation 更快？

可能直觉会觉得：显存少 ≠ 计算快

但 LLM inference 很多时候受到：**memory bandwidth** 限制。

每生成一个新 token，Attention 需要不断读取：历史 K/V

如果 KV Cache 很大：GPU 要搬很多数据

如果 KV Cache 变小：

```text
需要从显存读取的数据减少
```

所以可能同时获得：

```text
更少显存
+
更高 inference throughput
```

因此 GQA/MQA/MLA 不只是“省钱”，也是 inference optimization。

---

# 21. 用“图书馆”统一理解四种 Attention

这是最好记的类比。

## MHA：每个人一个图书馆

```text
8 个研究员

每个人：
自己的索引 K
自己的书 V
```

优点：

```text
自由度最高
```

缺点：

```text
8 套图书馆
太占地方
```

---

## GQA：几个人共用一个图书馆

```text
研究员 1,2 → 图书馆 A
研究员 3,4 → 图书馆 B
研究员 5,6 → 图书馆 C
研究员 7,8 → 图书馆 D
```

优点：

```text
省很多空间
还保留多个资料库
```

---

## MQA：所有人共用一个图书馆

```text
研究员 1～8
       ↓
同一个图书馆
```

最省。

但是：

```text
所有人的 K/V information 都必须共享
```

---

## MLA：不存完整图书馆，存压缩数据库

不是：

```text
保存很多完整 K/V
```

而是：

```text
把资料压缩
↓
保存 compact latent representation
↓
需要时通过 learned projections 使用
```

所以 MLA 的核心词：**Compression**

---

# 22. 这和你之前写的 q_kT_v() 有什么关系？

你之前实现：

```python
q = self.q_proj(x)
k = self.k_proj(x)
v = self.v_proj(x)
```

然后：

```text
q [B,H,S,HD]
k [B,H,S,HD]
v [B,H,S,HD]
```

这是标准 MHA 的思路：

```text
每个 head
都有自己的 Q/K/V slice
```

如果变成 GQA：

```text
Q heads = H

KV heads = G
```

其中：

$$
G < H
$$

多个 Q heads 会 map 到同一个 K/V head。

如果 MQA：

$$
G=1
$$

所有 Q heads 共享一个 K/V head。

所以你可以把它理解成： **你之前写的 Attention 数学没有被推翻，只是 K/V 的 head organization 和 caching strategy 改了。**

---

# 23. 这和 causal generation 的关系

假设 prompt：

```text
I love machine learning because
```

已经处理完。

KV Cache：

```text
I        → cached K,V
love     → cached K,V
machine  → cached K,V
learning → cached K,V
because  → cached K,V
```

现在生成：it


只需要：

```text
计算 "it" 的 Q/K/V

Q_it
↓
attend to cached historical K
↓
weighted sum of cached historical V
```

然后把：

```text
K_it
V_it
```

加入 cache。下一步继续。所以 sequence 越长：KV Cache 越大

这就是为什么 GQA/MQA/MLA 在现代长上下文 LLM inference 中非常重要。

---

# 24. 最重要的演进路线

把这一节压缩成：

```text
Standard MHA

每个 Q head
都有自己的 K/V
        ↓
quality/capacity 好
但 KV Cache 大


        ↓


GQA

多个 Q heads
共享一套 K/V
        ↓
KV Cache 减少
通常保留较好的 quality


        ↓


MQA

所有 Q heads
共享一套 K/V
        ↓
KV Cache 更小
压缩更激进


        ↓


MLA

把 K/V information
压缩到 latent space
        ↓
cache compressed latent
        ↓
进一步优化 KV Cache
```

---

# 25. 一张表总结

| Mechanism | Query Heads | KV Heads / Storage Idea | 大白话 |
|---|---:|---:|---|
| MHA | many | many | 每人一套资料 |
| GQA | many | several groups | 几个人共用一套资料 |
| MQA | many | one | 所有人共用一套资料 |
| MLA | many | compressed latent KV | 不存完整资料，存压缩版 |

---

# 26. 面试：What is KV Cache?

> **KV cache stores the key and value representations of previous tokens during autoregressive generation, so the model does not need to recompute them at every decoding step.**

大白话：

```text
历史 K/V 算过一次
→ 保存起来
→ 后面直接复用
```

---

# 27. 面试：Why is KV Cache expensive?

> **The KV cache grows with sequence length, number of layers, batch size, and the number and dimension of KV heads, so it can consume substantial GPU memory during long-context generation.**

---

# 28. 面试：What is Grouped-Query Attention?

> **Grouped-Query Attention keeps multiple query heads but lets groups of query heads share key and value heads, reducing KV-cache memory while retaining more capacity than sharing a single KV head across all queries.**

---

# 29. 面试：MHA vs GQA vs MQA

> **In standard MHA, every query head has its own key and value head. In GQA, groups of query heads share key/value heads. In MQA, all query heads share a single key/value head.**

直接记：

```text
MHA:
many Q → many KV

GQA:
many Q → several KV

MQA:
many Q → one KV
```

---

# 30. 面试：What is MLA?

> **Multi-Head Latent Attention compresses key-value information into a lower-dimensional latent representation and caches that compressed representation instead of conventional full per-head K/V states, substantially reducing KV-cache requirements.**

---

# 31. 面试：Why can smaller KV Cache speed up inference?

> **Autoregressive attention repeatedly reads cached keys and values from GPU memory. Reducing the KV cache lowers memory traffic and memory capacity requirements, which can improve generation throughput, especially for long contexts and large batches.**

---

# 32. 最终 Mental Model

如果考试前只剩一分钟，就记：

```text
Q = 当前在问什么

K = 历史信息的索引

V = 历史信息的内容


KV Cache
= 把历史 K/V 保存起来，不要重复计算


MHA
= 每个 Query head 一套 K/V

GQA
= 几个 Query heads 共用一套 K/V

MQA
= 所有 Query heads 共用一套 K/V

MLA
= 连完整 K/V 都不直接存，存压缩后的 latent KV
```

整个 Efficient Attention 这一部分可以浓缩成一句话： **大家都还在做 Attention，只是在想办法少保存一些历史 K/V，从而让 LLM generation 更省显存、更高效。**
