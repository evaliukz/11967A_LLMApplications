# RNN-based Language Models

## 1. RNN 是什么？

**RNN = Recurrent Neural Network，循环神经网络。**

它最核心的思想是：

> 当前时刻不仅看当前输入，还会把上一个时刻的 **hidden state** 传过来，作为过去信息的压缩表示。

对于 sequence：

```text
y1, y2, y3, ...
```

RNN 会按顺序处理：

```text
y1 → RNN → h1
           ↓
y2 → RNN → h2
           ↓
y3 → RNN → h3
```

这里：

* $y_t$：第 $t$ 个 token 的 embedding
* $h_t$：第 $t$ 步的 **hidden state**
* $h_t$ 表示“到目前为止 model 记住的 context”

---

# 2. RNN Decoder 的基本结构

在一个 RNN-based decoder 中，每一步 RNN 会接收两个主要输入：

1. 当前 token 的 embedding
2. 前一步的 hidden state

即：

$$
h_t = \text{RNN}(y_t, h_{t-1})
$$

更直观地看：

```text
previous hidden state h_(t-1)
            ↓
        ┌───────┐
y_t ───→│  RNN  │
        └───────┘
            ↓
          h_t
```

所以 RNN 的“memory”来自：

$$
h_{t-1}
$$

它把之前 sequence 的信息向后传递。

---

# 3. Initial Hidden State

第一步没有 previous hidden state，因此通常：

$$
h_0 = \mathbf{0}
$$

也就是初始化成 **zero vector**。

因此：

```text
h0 = zero vector
```

然后：

$$
(y_1,h_0) \rightarrow h_1
$$

再：

$$
(y_2,h_1) \rightarrow h_2
$$

不断继续。

---

# 4. RNN 每一步输出什么？

课件里的 RNN Decoder 每一步会产生两个东西：

### 1. Updated Hidden State

$$
h_t
$$

用于下一时间步。

### 2. Predicted Embedding

$$
\hat e_t
$$

用于预测下一个 token。

因此：

```text
Current embedding y_t
        +
Previous hidden h_(t-1)
        ↓
       RNN
     ↙     ↘
  h_t      ê_t
  ↓         ↓
next step   predict next token
```

---

# 5. Predicted Embedding 如何变成 Token Probability？

RNN 输出：

$$
\hat e_t
$$

这是一个 continuous vector，还不是 token。

之前我们学过，要通过 vocabulary embedding matrix 得到 **logits**：

$$
\text{logits} = E\hat e_t
$$

然后：

$$
P(Y_{t+1}=i)
= \text{softmax}(\text{logits})_i
$$

所以完整流程是：

```text
y_t + h_(t-1)
      ↓
     RNN
      ↓
     h_t
      ↓
Predicted Embedding ê_t
      ↓
Vocabulary Projection
      ↓
Logits
      ↓
Softmax
      ↓
P(next token)
```

---

# 6. 为什么 Hidden State 很重要？

RNN 的关键在于：

$$
h_t
$$

不是只包含当前 token 的信息。

它理论上包含：

```text
y1, y2, ..., y_t
```

整个 sequence 到目前为止的信息。

因此：

$$
h_t = f(y_1,y_2,\dots,y_t)
$$

可以把它理解成：

> **Hidden state = compressed memory of the sequence so far.**

---

# 7. 多层 RNN：Stacked RNN

实际 RNN 不一定只有一层，也可以有多层：

```text
Layer 2 RNN
    ↑
Layer 1 RNN
    ↑
token embedding
```

例如两层 RNN：

```text
          h_t^2
            ↑
        RNN Layer 2
            ↑
          h_t^1
            ↑
        RNN Layer 1
            ↑
           y_t
```

---

# 8. 第一层 Hidden State

对于第一层：

$$
h_t^1
=\text{RNN}
\left(
W_{ih}^1 y_t
+
W_{hh}^1 h_{t-1}^1
+
b_h^1
\right)
$$

含义：

当前 hidden state 由两部分信息决定：

### 当前输入

$$
W_{ih}^1y_t
$$

### 上一时间步 hidden state

$$
W_{hh}^1h_{t-1}^1
$$

再加上：

$$
b_h^1
$$

bias。

---

# 9. Higher RNN Layers

对于更高层 $l$：

$$
h_t^l
=\text{RNN}
\left(
W_{ih}^l y_t
+
W_{lh}^{l} h_t^{l-1}
+
W_{hh}^l h_{t-1}^l
+
b_h^l
\right)
$$

直觉上，第 $l$ 层要同时看到：

* 当前 token 的信息
* lower layer 当前 step 的 hidden state
* 自己上一 step 的 hidden state

因此它同时在：

```text
vertical direction
```

跨 layer 传信息，

以及：

```text
horizontal direction
```

跨 time step 传信息。

---

# 10. RNN 的两个方向

这个概念非常重要。

### Across Time

```text
h1 → h2 → h3 → h4
```

这是 RNN 的 **recurrent connection**。

负责记住 sequence history。

### Across Layers

```text
Layer 1
   ↓
Layer 2
   ↓
Layer 3
```

负责让 representation 变得更复杂。

所以 stacked RNN 同时存在：

> **temporal depth + network depth**

---

# 11. 最终 Prediction

如果有 $L$ 层 RNN，可以综合 hidden states 得到 predicted embedding：

$$
\hat e_t
=b_e
+
\sum_{l=1}^{L}
W_{h^le} h_t^l
$$

其中：

* $h_t^l$：第 $l$ 层、第 $t$ 时刻的 hidden state
* $W$：learned weight matrix
* $b_e$：learned bias

这些：

$$
W,;b
$$

都是训练过程中学习出来的 **learned parameters**。

---

# 12. RNN Encoder

RNN 同样可以作为 **Encoder**。

假设输入 sequence：

$$
X=(x_1,x_2,\dots,x_T)
$$

Encoder 按顺序处理：

```text
x1 → RNN → h1_enc
            ↓
x2 → RNN → h2_enc
            ↓
...
            ↓
xT → RNN → hT_enc
```

最终：

$$
h_T^{enc}
$$

就是处理完整 input sequence 后的 final hidden state。

---

# 13. RNN Encoder-Decoder 如何连接？

早期最简单的 Seq2Seq 方法：

> 把 Encoder 最后的 hidden state，直接作为 Decoder 的 initial hidden state。

也就是：

$$
h_0^{dec}
=h_T^{enc}
$$

流程：

```text
Input Sequence X
      ↓
    Encoder
      ↓
h_T^enc
      ↓
Initialize Decoder
      ↓
    Decoder
      ↓
Target Sequence Y
```

---

# 14. 为什么这样可以工作？

Encoder 的 final hidden state：

$$
h_T^{enc}
$$

理论上已经看过：

$$
x_1,x_2,\dots,x_T
$$

所以它被认为是整个 input sequence 的压缩表示。

因此：

> Encoder compresses the source sentence into one fixed-size vector.

Decoder 再从这个 vector 开始生成目标语言。

例如：

```text
French sentence
      ↓
RNN Encoder
      ↓
one hidden vector
      ↓
RNN Decoder
      ↓
English sentence
```

---

# 15. 这个方法的问题：Information Bottleneck

问题非常明显：

> 整个 input sentence 的所有信息，都必须塞进一个固定长度的 vector：

$$
h_T^{enc}
$$

如果 input 很短还可以。

但如果 input 很长：

```text
x1 x2 x3 ... x100
```

Decoder 仍然只能拿到：

```text
one vector
```

这就形成：

**Information Bottleneck**

---

# 16. 为什么长 Sequence 特别困难？

比如翻译：

```text
French:
"L'hippopotame a mangé mes devoirs ..."
```

Encoder 要把整句话：

```text
word1
word2
word3
word4
word5
...
```

全部压缩进：

$$
h_T^{enc}
$$

然后 Decoder 第 20 步还必须从这个 vector 中“记起”source sentence 某个早期 word。

这很困难。

因此早期 RNN Seq2Seq：

> 长 sequence translation performance 明显下降。

---

# 17. 更好的方法：Attention Mechanism

解决方法就是：

# Attention

核心思想：

> 不要只给 Decoder 一个 final encoder hidden state。

而是让 Decoder 在生成每个 token 时：

> **重新查看所有 Encoder hidden states，并决定当前应该关注哪些 input tokens。**

---

# 18. Encoder 不再只提供一个 Hidden State

原来的方法：

```text
Encoder
   ↓
h_T
```

Attention 方法保留：

$$
h_1^{enc},
h_2^{enc},
\dots,
h_T^{enc}
$$

也就是每一个 input token 的 contextual representation。

---

# 19. Attention 的核心问题

在 Decoder 生成第 $t$ 个 token 时：

> source sequence 中哪些 token 对当前 prediction 最重要？

例如 French → English：

```text
French:
L' hippopotame a mangé mes devoirs
```

如果现在 Decoder 要生成：

```text
hippopotamus
```

模型应该重点关注 source 中：

```text
hippopotame
```

而不是：

```text
mes
```

或：

```text
devoirs
```

---

# 20. Attention Weight

Attention 会给 source sequence 中每个 token 一个 weight：

$$
\alpha_{t,1},
\alpha_{t,2},
\dots,
\alpha_{t,T}
$$

通常：

$$
\sum_{i=1}^{T}\alpha_{t,i}=1
$$

例如：

```text
L'             0.02
hippopotame    0.80
a              0.05
mangé          0.07
mes            0.03
devoirs        0.03
```

表示：

> 当前预测 English token 时，模型 80% 的 attention 放在 `hippopotame` 上。

---

# 21. Context Vector

利用 Attention weights，对 encoder hidden states 做 weighted sum：

$$
c_t
=\sum_{i=1}^{T}
\alpha_{t,i}
h_i^{enc}
$$

得到：

$$
c_t
$$

称为：

**Context Vector**

注意这里的 context vector：

> 每一个 decoder time step 都可以不同。

---

# 22. 为什么 Attention 比 Final Hidden State 好？

旧方法：

$$
X
\rightarrow
h_T^{enc}
\rightarrow
Decoder
$$

整个 source 永远压成一个 vector。

Attention：

$$
h_1^{enc},\dots,h_T^{enc}
$$

都可以被 Decoder 访问。

而且每一步 Decoder 可以动态决定：

```text
Which input token matters right now?
```

因此：

> Attention removes the fixed-vector bottleneck.

---

# 23. 一个翻译例子

Source：

```text
L' hippopotame a mangé mes devoirs
```

Target：

```text
The hippopotamus ate my homework
```

生成：

```text
The
```

可能主要关注：

```text
L'
```

生成：

```text
hippopotamus
```

主要关注：

```text
hippopotame
```

生成：

```text
ate
```

主要关注：

```text
mangé
```

生成：

```text
homework
```

主要关注：

```text
devoirs
```

所以 Attention 也可以看成：

> 一种 **soft alignment** between source and target tokens.

这正是早期 machine translation 中 Attention 非常重要的原因。

---

# 24. RNN Encoder-Decoder 的演进

可以把历史发展记成：

```text
RNN Language Model
       ↓
RNN Encoder–Decoder
       ↓
Final Hidden State Bottleneck
       ↓
RNN + Attention
       ↓
Transformer
```

Attention 最开始是：

> RNN architecture 上的一个辅助机制。

后来 Transformer 做了一个非常关键的变化：

> **直接去掉 recurrence，让 Attention 成为核心计算机制。**

---

# 25. RNN 的主要问题

RNN 很重要，但现代 LLM 已经基本不用 vanilla RNN 作为核心 architecture。

主要有几个原因。

### 1. Sequential Computation

必须：

```text
h1 → h2 → h3 → h4
```

$h_4$ 必须等 $h_3$ 算完。

因此很难 parallelize。

而 Transformer 可以同时处理很多 tokens。

---

### 2. Long-Term Dependency

理论上：

$$
h_t
$$

可以携带所有历史信息。

实际上随着 sequence 变长：

> 很早以前的信息越来越难保留。

---

### 3. Vanishing / Exploding Gradients

训练时使用：

**Backpropagation Through Time (BPTT)**

梯度需要沿着：

```text
h100 → h99 → ... → h1
```

不断传播。

容易出现：

* **Vanishing Gradient**
* **Exploding Gradient**

因此很难学习非常 long-range dependencies。

---

# 26. LSTM / GRU

为了解决 vanilla RNN 的 memory 问题，后来出现：

* **LSTM — Long Short-Term Memory**
* **GRU — Gated Recurrent Unit**

通过 gates 控制：

```text
what to remember
what to forget
what to update
```

它们比 vanilla RNN 更擅长 long-term dependencies。

但仍然有：

> sequential computation bottleneck

因此最终 Transformer 逐渐成为主流。

---

# 27. 面试：RNN 的核心公式

最基础形式：

$$
h_t
===

f(W_x x_t + W_h h_{t-1}+b)
$$

其中：

* $x_t$：current input
* $h_{t-1}$：previous hidden state
* $h_t$：new hidden state
* $W_x,W_h$：learned weights
* $f$：activation function，例如 tanh

核心理解：

> Current hidden state combines the current input with information carried from previous time steps.

---

# 28. 面试：Encoder-Decoder 如何连接？

早期 RNN Seq2Seq：

$$
h_0^{dec}=h_T^{enc}
$$

即：

> The final hidden state of the encoder initializes the decoder hidden state.

问题：

> All source information is compressed into one fixed-size vector, creating an information bottleneck.

---

# 29. 面试：Attention 为什么出现？

最标准的回答：

> In early RNN encoder-decoder models, the entire source sequence had to be compressed into a single fixed-size hidden state. This created an information bottleneck, especially for long sequences. Attention allows the decoder to dynamically access and weight all encoder hidden states at each decoding step.

中文：

> RNN Seq2Seq 最大的问题是 encoder 必须把整个输入压缩进一个 fixed-size vector。Attention 允许 decoder 每一步直接查看所有 encoder hidden states，并动态选择当前最相关的信息。

---

# 30. RNN vs Attention 的直觉

## No Attention

```text
Source
 ↓
Encoder
 ↓
ONE vector
 ↓
Decoder
 ↓
Target
```

问题：

```text
Everything must fit inside ONE vector.
```

## With Attention

```text
Source Tokens
 ↓ ↓ ↓ ↓ ↓
Encoder Hidden States
 ↓ ↓ ↓ ↓ ↓
Attention
 ↘ ↓ ↙
Context for current decoder step
 ↓
Decoder
```

Decoder 每一步都可以重新查 source。

---

# 31. 本节最重要的知识链

```text
Token Embedding y_t
        +
Previous Hidden State h_(t-1)
        ↓
       RNN
        ↓
Current Hidden State h_t
        ↓
Predicted Embedding
        ↓
Logits
        ↓
Softmax
        ↓
Next Token Probability
```

对于 Seq2Seq：

```text
Input
 ↓
RNN Encoder
 ↓
Encoder Hidden States
 ↓
Attention
 ↓
RNN Decoder
 ↓
Output
```

---

# Interview Cheat Sheet

### What is an RNN?

> An RNN processes a sequence recurrently. At each time step, it combines the current input with the previous hidden state to produce a new hidden state.

### What is the hidden state?

> A hidden state is a compressed representation of the sequence processed so far.

### How did early Seq2Seq RNNs connect encoder and decoder?

> The final encoder hidden state was used to initialize the decoder hidden state.

### What was the problem?

> It created a fixed-size information bottleneck, especially for long sequences.

### Why Attention?

> Attention lets the decoder dynamically access all encoder hidden states instead of relying on one fixed representation.

### Main weakness of RNNs?

> RNNs are inherently sequential, difficult to parallelize, and struggle with long-range dependencies due to issues such as vanishing gradients.

---

# 一句话总结

> **RNN carries sequence history through a recurrent hidden state; early RNN encoder-decoder models compressed the entire input into one final hidden state, while Attention improved this by letting the decoder dynamically retrieve relevant information from all encoder hidden states.**
