
# MultiHeadAttention，实现Transformer的self attention，让模型关注输入中的不同部分

流程 X → Q/K/V → split heads → QKᵀ/√d_k → softmax → weighted V → concat heads → output projection


# 1. Dimension symbols:
<pre>
    B - batch size
    S - sequence length
    D - hidden dimension (n_embd)
    H - number of attention heads (n_head)
    HD - hidden dimension of a single attention head (d // n_head)
    V - size of the vocabulary
</pre>

这几个变量其实就是你做 Multi-Head Transformer 时最重要的一套 shape vocabulary。

一个 Transformer 输入通常可以看成一个三维 tensor：[B, S, D] 也就是：batch 里有 B 个句子，每个句子有 S 个 token，每个 token 用 D 维向量表示。

## B = batch size

表示一次 forward pass 里，同时处理多少条 sequence。

例如你有 4 个句子一起送进模型：

"I love cats", 
"She likes coffee", 
"Transformers are powerful", 
"Hello world"

那么：B = 4

如果每条都 padding / truncation 到长度 8，那么输入 tensor 可能是 x.shape = [4, 8, D]

其中第一维就是 B=4。

你可以理解成：B 只是“同时并行处理多少个样本”。

它不会改变 Transformer 的核心数学逻辑，只是为了 training efficiency。

## S = sequence length

表示：一条 sequence 里有多少个 token position。

例如："I love machine learning"

假设 tokenize 后：["I", "love", "machine", "learning"]， 那么：S = 4

当然现实里通常会有：padding，BOS，EOS，subword tokenization，所以实际 S 是 token 数，不一定是 word 数。

例如："unbelievable"，可能被切成多个 subword token。

为什么 S 很重要？Self-Attention 要让每个 token 和所有 token 比较。

因此 attention score matrix 大小是：S × S

如果：S = 128 那么每个 head 的 attention matrix：128 × 128

如果：S = 4096 则：4096 × 4096 所以 vanilla attention 的复杂度大约是：O(S²)， 这就是长 context 很贵的根本原因之一。

## D = hidden dimension = n_embd

这是最核心的维度之一。它表示：每个 token 在 Transformer 内部用多少维向量表示。

例如：D = 768，那么每个 token 是一个 768 维向量：token → [0.12, -0.43, ..., 0.88] 长度是 768。

所以整个 batch 的 hidden states：x.shape = [B, S, D] 例如：B = 32，S = 128，D = 768 那么：x.shape = [32, 128, 768]

D 到底代表什么？可以理解成：每个 token 的“representation capacity”。

这个向量里可能编码：syntax，semantics，position-related context，entity relationships，other learned features 但不要理解成“某一维固定代表某个概念”。

D 在 Transformer 里通常贯穿始终，例如：Embedding output → [B, S, D]， Attention output → [B, S, D]  FFN output → [B, S, D]

Residual connection 之所以可以直接：x + attention_output 就是因为 shape 必须一样：[B, S, D]

## H = number of attention heads = n_head

表示：一个 Multi-Head Attention 里有多少个 attention head。

例如：H = 12意味着同时运行 12 个不同的 attention mechanism。每个 head 可以学习不同的 relationship pattern。

例如某些 head 可能偏向：local context，subject-verb relation，coreference，punctuation pattern，long-range dependencies

不过这只是直觉，不要认为每个 head 都有人类可解释语义。

## HD = hidden dimension per head

通常：HD = D // H 因为整个 hidden dimension D 被分给 H 个 heads。

例如：D = 768 H = 12 那么：HD = 768 // 12 = 64  所以：12 heads × 64 dimensions = 768

也就是：H × HD = D 这是一个特别重要的关系。

为什么要拆成 Heads？

输入原本：[B, S, D] 比如：[B, S, 768]

Multi-Head Attention 会 reshape 成：[B, S, H, HD] 例如：[B, S, 12, 64]

通常接着 transpose 成： [B, H, S, HD]

为什么这样做？因为这样每个 head 就可以独立做 attention。

你可以把 D 想成被切成 H 份，比如：D = 8，H = 2，HD = 4

一个 token 的 hidden vector：[a b c d e f g h] 可以 conceptualize 成：Head 1: [a b c d]  Head 2: [e f g h]

实际代码里不是简单机械切 input，而通常先经过 learned linear projections：Q = x @ Wq， K = x @ Wk， V = x @ Wv 然后再 reshape 成多个 heads。

## V = Vocabulary Size

这里的 V 和 Attention 里的 Value V 很容易混淆。你这里写的：V = size of vocabulary 代表tokenizer 中一共有多少种可能 token。

例如V = 50_000 意味着模型 vocabulary 有 50,000 个 token。

Vocabulary Size 在哪里用？

**最终 Transformer hidden state：[B, S, D] 还不是 token probability。需要 Linear projection：D → V  所以：logits.shape = [B, S, V]**

例如：[B, S, 50_000] 含义：每个 batch 中，每个 token position，都有 50,000 个 vocabulary token 的 score。

然后：softmax(logits, dim=-1)

得到：P(next token)

Vocabulary V 和 Attention Value V 不一样，面试 / 写代码时一定注意。

通常：V_vocab，表示 vocabulary size。

而：V，在 Attention 公式里表示 Value matrix。

Attention：Attention(Q, K, V) 这里 V 不是 vocabulary。所以你自己的笔记里我建议写：VOCAB_SIZE 或者：Vocab，避免和 Value 混淆。

### Vocabulary 到底是什么？为什么 Transformer 最后一定要从 hidden vector 再“投影”到 vocabulary 上？

我用一个非常具体的小例子，从输入一句话开始，把每一步的 output 长什么样都走一遍。

先说结论，假设我们的 toy model 只有 8 个 token：
<pre>
Vocabulary:
0: <BOS>
1: I
2: like
3: cats
4: dogs
5: pizza
6: you
7: <EOS>
</pre>

这整个表就叫：Vocabulary 也就是：模型所有“允许输出”的 token 候选集合。

现实里的 LLM vocabulary 可能有：50,000、100,000、200,000 甚至更多 token。

Vocabulary 可以理解成“词库菜单”，假设我要让模型完成：

I like ...

它最后必须回答：下一个 token 是什么？

但模型不能输出一个神秘的 4 维向量：[0.21, -1.3, 0.77, 2.1] 用户没法理解这个。

最终必须从模型认识的 token 里选一个：

cats
dogs
pizza
you
<EOS>
...

所以模型最后一定需要做：
<pre>
内部向量
   ↓
给 vocabulary 中每个 token 打分
   ↓
哪个 token 最可能？

这就是为什么要 project to vocabulary。
</pre>

# 2. 整个 Transformer 数据形状流：

<pre>
Token IDs
[B, S]
   ↓
Embedding
[B, S, D]
   ↓
Q/K/V Projection
[B, S, D]
   ↓
Split Heads
[B, H, S, HD]
   ↓
Attention Scores
[B, H, S, S]
   ↓
Attention Output
[B, H, S, HD]
   ↓
Concat Heads
[B, S, D]
   ↓
Transformer Layers
[B, S, D]
   ↓
LM Head
D → Vocab
   ↓
Logits
[B, S, Vocab]
</pre>

# 3. 用一个例子走完整个transformer数据流

从最开始走一遍，假设：
<pre>
B = 1
S = 2
D = 4
Vocab = 8
</pre>

## 输入：I like, tokenizer 先查 vocabulary。

得到：I → 1, like → 2

所以最开始输入模型的是：[1, 2]  Shape：[B, S] = [1, 2]

注意现在它只是 integer ID。

## Step 1：Token IDs → Embeddings

模型不能直接拿：1 2 去做神经网络计算。 所以要去 Embedding Matrix 查表。

假设：

I    → [1.0, 0.2, -0.1, 0.5]

like → [0.4, 1.2, 0.3, -0.2]

于是： "I like" 变成：
<pre>
[
  [1.0, 0.2, -0.1, 0.5],   # I
  [0.4, 1.2,  0.3,-0.2]    # like
]

Shape：
[B, S, D]
[1, 2, 4]
</pre>

现在 D=4 到底是什么意思？ 就是：每个 token 用 4 个数字表示。

所以：I不是1,而是[1.0, 0.2, -0.1, 0.5]这些数字不是人为定义的。是模型训练出来的 representation。

## Step 2：经过 Transformer

接下来：
<pre>
Embeddings
   ↓
Self-Attention
   ↓
Feed Forward
   ↓
...
</pre>
最关键的变化是：每个 token 的 vector 开始包含上下文信息。

原来 like 的 embedding：[0.4, 1.2, 0.3, -0.2]只是 token "like" 本身的表示。

经过 Transformer 后，可能变成：[1.8, -0.5, 2.1, 0.7] 这个 vector 可以理解成 “这是出现在 I like 这个上下文里的 like。”

所以 Transformer output 可能是：
<pre>
[
  [0.8, 0.1, 1.4, -0.3],   # contextual representation of I
  [1.8,-0.5, 2.1,  0.7]    # contextual representation of like
]

Shape 还是：[1, 2, 4]也就是[B, S, D]
</pre>

这里非常关键：Transformer 输出还不是 word. 最后一个 token "like" 对应：h = [1.8, -0.5, 2.1, 0.7]

这个 h 是：hidden state / hidden representation

它可以理解为：模型把目前看到的 "I like" 总结成了一个 4 维的“想法”。

但是现在我们还有一个问题：[1.8, -0.5, 2.1, 0.7] 到底意味着下一个词是什么？人看不出来。模型也还没真正做出 categorical prediction。

# Step 3: 于是要 Project to Vocabulary

现在 vocabulary 有 8 个 token：
</pre>
<BOS>
I
like
cats
dogs
pizza
you
<EOS>
</pre>

所以我们希望模型给这 8 个 token：每一个都打一个分。也就是从：4 个数字变成 8 个数字

所以要做一个 Linear Layer：D → Vocabulary Size

这里：4 → 8, 数学上：

hidden state ×  W    =  logits
[4]          [4, 8]     [8]


Projection 之后变成什么？

假设：h = [1.8, -0.5, 2.1, 0.7]

经过：logits = h @ W_vocab + b

得到：
<pre>
<BOS>   -4.2
I       -1.1
like    -2.3
cats     3.7
dogs     2.9
pizza    2.1
you     -0.8
<EOS>   -1.5
</pre>
    
这就是：logits, 现在每个 vocabulary token 都有一个 score。

这就是“project to vocabulary”的真正含义, 不是说：把 vector 变成一个 word。

而是：把 hidden vector 转成“对 vocabulary 中每个 token 的一套分数”。

从hidden space变成vocabulary score space

比如：[1.8, -0.5, 2.1, 0.7]

变成：
<pre>
[
-4.2,   # <BOS>
-1.1,   # I
-2.3,   # like
 3.7,   # cats
 2.9,   # dogs
 2.1,   # pizza
-0.8,   # you
-1.5    # EOS
]
</pre>

为什么输出长度一定是 Vocabulary Size？

因为最终模型必须回答：vocabulary 里的每一个 token，当 next token 的可能性是多少？

假设：Vocabulary Size = 50,000, 那么模型最后就必须输出：50,000 个 logits 每一个对应一个 token。

所以：

hidden state shape:
[D]

要变成：

logits shape:
[Vocab]

## Step 4：Logits → Softmax

刚才这些：
<pre>
cats     3.7
dogs     2.9
pizza    2.1
...
</pre>
还不是 probability。

Softmax 后可能变成：
<pre>
<BOS>   0.0002
I       0.004
like    0.001
cats    0.55
dogs    0.25
pizza   0.15
you     0.03
<EOS>   0.0148
</pre>
所有 probability 加起来：1.0

现在模型才真正得到：P(next token | "I like")

# Step 5: 然后怎么生成？

比如：
<pre>
cats   55%
dogs   25%
pizza  15%
</pre>

如果用 greedy：choose cats

于是 sequence：I like cats

然后把 cats 再送回模型：I like cats

继续预测：P(next token | "I like cats")

比如：

<pre>
<EOS>    0.60
and      0.15
because  0.08
...
</pre>

再选下一个。这就是 autoregressive generation。

# 整个过程你可以理解成“翻译了三次”

第一层翻译：

Text
"I like"

变成：

Token IDs
[1, 2]

第二层翻译：Token IDs

变成：

Neural Network Vectors
[B,S,D]

第三层翻译：Hidden Vector

变回：Scores over human-readable vocabulary

也就是：

cats  ?
dogs  ?
pizza ?
...

# 为什么不能直接让 Transformer output 就是 vocabulary？

理论上可以设计成那样，但实际上中间需要一个 rich hidden representation。

比如：D = 768

这 768 维空间负责：

理解 syntax
semantic relationships
context
entity information
long-range relationships
reasoning-related features

最后一步才问：好，现在根据这些内部信息，到底哪个 token 最合适？

所以分成：
<pre>
understand / represent
        ↓
hidden state
        ↓
choose vocabulary token
</pre>

一个特别好理解的类比

你可以把 Transformer 想成一个参加选择题考试的人。

题目：

I like ___

Transformer 内部思考了一遍，脑子里的 state 是：

[1.8, -0.5, 2.1, 0.7]

这个相当于：

“我理解这个句子了。”

但是考试最终要求你从选项里选：

A. cats
B. dogs
C. pizza
D. you

所以最后一个 Linear Layer 干的事情就是：

把“脑子里的理解”转换成“每个答案选项的分数”。

比如：

cats    9分
dogs    7分
pizza   6分
you     1分

然后 Softmax 变成：

cats    55%
dogs    25%
pizza   15%
you      5%

这就是 project to vocabulary。

16. 再来看 [B,S,D] → [B,S,V]

这是你做 Transformer 最应该理解的。

Transformer 出来：[B, S, D]

例如：[2, 4, 768]

意思：

2 个 sentence
每个 4 个 token
每个 token 有一个 768 维 hidden representation

经过 vocabulary projection：Linear(D, V)

假设：V = 50,000

就变成：[B, S, V] = [2, 4, 50000]

意思：对 batch 中每个 sentence 的每个 position，都预测了 50,000 个 token 的 logits。

# 为什么每个 position 都需要 [V]？

训练时，比如：<BOS> I like cats

其实模型可以同时学：

<BOS> → I
I     → like
like  → cats
cats  → EOS

所以每个 position 都要输出一个完整的 vocabulary distribution：

position 0:
P(next token = every vocab token)

position 1:
P(next token = every vocab token)

position 2:<pre>P(next token = every vocab token)

...

因此：[B,S,V]非常自然。

# 一个完整 toy example

Vocabulary：
<pre>
0 <BOS>
1 I
2 like
3 cats
4 dogs
5 pizza
6 you
7 <EOS>
</pre>

输入：I like

## Step 1 — Tokenize

"I like" → [1, 2]

Shape： [1,2]

## Step 2 — Embedding

1 → [1.0,0.2,-0.1,0.5]

2 → [0.4,1.2,0.3,-0.2]

变成：
<pre>
[
 [1.0,0.2,-0.1,0.5],
 [0.4,1.2,0.3,-0.2]
]
</pre>

Shape：[1,2,4]

## Step 3 — Transformer

经过 self-attention + FFN：
<pre>
[
 [0.8,0.1,1.4,-0.3],
 [1.8,-0.5,2.1,0.7]
]
</pre>

Shape：[1,2,4]

现在第二个 vector 表示："I like" context

## Step 4 — Vocabulary Projection

取最后一个 hidden state：[1.8,-0.5,2.1,0.7]

通过：Linear(4 → 8)

得到：[-4.2,-1.1,-2.3,3.7,2.9,2.1,-0.8,-1.5]

Shape：[8]

## Step 5 — Map Scores Back to Vocabulary

<pre>
<BOS>  -4.2
I      -1.1
like   -2.3
cats    3.7
dogs    2.9
pizza   2.1
you    -0.8
<EOS>  -1.5
Step 6 — Softmax
cats   0.55
dogs   0.25
pizza  0.15
...
</pre>

## Step 7 — Sampling

选择：cats

于是：I like cats

然后重新循环。

## 你现在可以这样理解 Vocabulary

Vocabulary 不是模型脑子里的知识。Vocabulary 只是：Tokenizer 定义的所有可能 token 的清单。

比如模型可能知道大量关于猫的知识，这些知识存在 model parameters / hidden representations 里。

Vocabulary 只是告诉模型：如果你最后想说 "cats"，它对应 token ID 18342。

所以：Vocabulary = output alphabet of the language model. 这个说法很好。

就像英语字母表：a-z 是 character-level model 的 output alphabet。

LLM 则拥有一个更大的 subword alphabet：

"The"
"ing"
"cat"
"##tion"
...

# 最关键的一句话

你现在把整个 Transformer 想成：

Transformer 的工作不是直接猜单词，而是把 context 变成一个有意义的 hidden vector；LM head 再把这个 hidden vector翻译成 vocabulary 中每个 token 的分数。

所以：

<pre>
Context
  ↓
Transformer
  ↓
Hidden Representation [D]
  ↓
Vocabulary Projection
  ↓
Logits [V]
  ↓
Softmax
  ↓
Probability over Vocab
  ↓
Next Token
</pre>
