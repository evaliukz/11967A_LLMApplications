# Transformer LayerNorm 改进：大白话理解 Pre-LN、Post-LN 和 RMSNorm

<img width="887" height="521" alt="image" src="https://github.com/user-attachments/assets/8a658111-203b-4d10-bb8f-dc18ec5e1588" />


<img width="1000" height="531" alt="image" src="https://github.com/user-attachments/assets/95d35bf6-4047-4df5-b0c0-96a5948cd02c" />

<img width="973" height="432" alt="image" src="https://github.com/user-attachments/assets/4c94d518-c247-4e93-9189-3f0ed7154d6b" />



> 这一节真正要回答的不是“LayerNorm 公式怎么背”，而是： **Transformer 堆得越来越深以后，怎么让每一层的数值和梯度不要越来越失控，让大模型更容易训练？**

先记住整个结论：

```text
LayerNorm = 把每个 token 的 hidden vector 整理到比较稳定的尺度

Post-LN = 先做 Attention/FFN + Residual，再整理

Pre-LN = 先整理，再送进 Attention/FFN → 通常更容易稳定训练深层 Transformer

RMSNorm = 更简单的 normalization
→ 不减均值，只控制整体大小
→ 计算更简单，现代 LLM 中很常见
```

---

# 1. 先回到你之前写过的 DecoderBlock

你之前的代码类似：

```python
ln_x = self.ln_1(x)
mha = self.mha(ln_x, attention_mask)
x = x + mha

ln_x = self.ln_2(x)
ff = self.ff(ln_x)
x = x + ff
```

这其实已经是：Pre-LN Transformer

因为顺序是：

```text
LayerNorm
↓
Attention
↓
Residual Add
```

以及：

```text
LayerNorm
↓
FFN
↓
Residual Add
```

公式：

$$
x_1=x+Attention(LN(x))
$$

$$
x_2=x_1+FFN(LN(x_1))
$$

所以你其实已经亲手写过这张 slide 讲的 modern improvement。

---

# 2. LayerNorm 到底是干什么？

假设某个 token 的 hidden vector 是：

```text
x = [2, 4, 6, 8]
```

这 4 个数字就是这个 token 当前的 4 个 hidden features。

随着它不断经过：

```text
Attention
Residual
FFN
Residual
Attention
Residual
FFN
Residual
...
```

数字的 scale 可能越来越难控制。

比如某一层：

```text
[2, 4, 6, 8]
```

另一层可能变成：

```text
[40, -30, 100, -80]
```

再深一些甚至可能更极端。

训练一个几十层、上百层的 Transformer 时，这种数值尺度变化会让 optimization 更困难。

所以 LayerNorm 很像：**每次进入重要计算之前/之后，先把这个 token 的 hidden features 整理一下，让它们保持在比较稳定的数值范围。**

---

# 3. 用“开会前整理状态”理解 LayerNorm

继续用之前的公司类比。

```text
Attention = 大家开会交换信息

FFN = 每个人回办公室独立处理信息
```

LayerNorm 就像： **进入会议或工作之前，把每个人的状态调整到一个比较统一、稳定的工作尺度。**

不是：LayerNorm 帮 token 学新知识

而是：

```text
LayerNorm = 稳定 representation

Attention / FFN = 真正进行主要的信息处理
```

一句话： **LayerNorm stabilizes; Attention and FFN transform.**

---

# 4. LayerNorm 的公式其实只做三件事

标准 LayerNorm 可以写成：

$$
LN(x_i)=
\gamma_i
\frac{x_i-\mu}{\sqrt{\sigma^2+\epsilon}}
+
\beta_i
$$

slide 为简化可能省略了 bias $\beta$ 和 $\epsilon$。

不要先背公式。

它本质上就是：

```text
① 算平均值
② 减掉平均值，再按标准差缩放
③ 用 learnable scale / bias 再调整
```

---

# 5. 一个真正的小例子

假设：x = [2, 4, 6, 8]


## Step 1：求平均

$$
\mu=\frac{2+4+6+8}{4}=5
$$

所以：mean = 5

---

## Step 2：减掉平均值

```text
[2,4,6,8]
-
[5,5,5,5]

=

[-3,-1,1,3]
```

现在 vector 被重新 centered around 0。

这一步叫：Re-centering

---

## Step 3：除以标准差

这里 variance：

$$
\sigma^2 =
\frac{
(-3)^2+(-1)^2+1^2+3^2
}{4} =
5
$$

所以：

$$
\sigma=\sqrt5\approx2.236
$$

除完：

```text
[-3,-1,1,3] / 2.236

≈

[-1.34,-0.45,0.45,1.34]
```

现在：

```text
mean ≈ 0
scale 比较统一
```

这就是 normalization。

---

# 6. 为什么最后还有 learnable parameter？

如果 LayerNorm 永远强制：

```text
mean = 0
scale = 1
```

可能限制模型。

所以又给模型 learnable parameters：

```text
γ (gamma)
β (beta)
```

让模型可以学习：

> “标准化以后，我到底应该重新放大多少、移动多少？”

所以：

```text
Normalize
不是把模型能力锁死

而是先整理
再允许模型学习合适的 scale/shift
```

---

# 7. 最重要的问题：LayerNorm 放在哪里？

这就是：

```text
Post-LN
vs
Pre-LN
```

---

# 8. Post-LN：原始 Transformer 的经典设计

Post-LN：

```text
x
│
├───────────────┐
│               │
│          Attention
│               │
└────── + ──────┘
        ↓
    LayerNorm
        ↓
      output
```

公式：

$$
y=LN(x+Attention(x))
$$

意思是：

```text
先：
Attention

再：
Residual Add

最后：
LayerNorm
```

所以叫：Post-LN

因为 normalization 在 sublayer/residual addition **之后**。

---

# 9. Pre-LN：现代深层 Transformer 常见设计

Pre-LN：

```text
x ───────────────────┐
│                    │
↓                    │
LayerNorm             │
↓                    │
Attention              │
↓                    │
└──────── + ──────────┘
           ↓
         output
```

公式：

$$
y=x+Attention(LN(x))
$$

也就是：

```text
先：LayerNorm

再：Attention

最后：和原来的 x 做 Residual Add
```

所以叫 Pre-LN

---

# 10. Post-LN 和 Pre-LN 最值得记的区别

## Post-LN

$$
y=LN(x+F(x))
$$

```text
先干活
↓
Residual Add
↓
再整理
```

---

## Pre-LN

$$
y=x+F(LN(x))
$$

```text
先整理
↓
再干活
↓
Residual Add
```

这里：F可以是：Attention， 也可以是：FFN

---

# 11. 为什么 Pre-LN 往往更稳定？

这是这张 slide 最核心的内容之一。

看 Pre-LN：

$$
y=x+F(LN(x))
$$

注意这里有一条非常干净的路径：

```text
x
│
│  什么复杂计算都不用经过
│
└────────────→ +
```

也就是 residual path。

在 backward propagation 时，gradient 也有一条比较直接的路径可以往前传播。

大白话 **即使 Attention / FFN 那条复杂路线的 gradient 不太稳定，Residual 仍然提供了一条“高速公路”。**

这对非常深的网络尤其重要。

---

# 12. 用“高速公路”理解 Residual + Pre-LN

假设有 100 个 Transformer blocks。

没有好的 residual/normalization 设计时：

```text
gradient
↓
第100层
↓
复杂变换
↓
第99层
↓
复杂变换
↓
...
↓
第1层
```

一路经过大量 transformation。

容易：

```text
变得很大
或者
变得很小
```

Residual path 相当于：

```text
第100层
│
├──复杂道路
│
└──高速公路
      ↓
第99层
│
├──复杂道路
│
└──高速公路
...
```

gradient 有直接路径传播。Pre-LN 的排列方式通常让深层 Transformer 的 optimization 更友好。

---

# 13. Slide 中 Gradient L2 Norm 图怎么看？

你不需要背数值。

它想表达：

```text
Post-LN
→ gradient scale 可能较大 / optimization 更敏感

Pre-LN
→ gradient behavior 更温和
→ 深层训练通常更稳定
```

所以现代 large-scale Transformer architecture 经常偏向：

```text
Pre-Norm style
```

而不是原始 2017 Transformer 的 classic Post-LN。

---

# 14. 现在进入 RMSNorm

第二张 slide 又问： **既然 LayerNorm 是为了稳定 scale，那我们真的需要做这么多事情吗？**

标准 LayerNorm：

```text
① 求 mean
② 减 mean
③ 求 variance / standard deviation
④ 除以 std
⑤ learnable scale
```

RMSNorm 的想法：**也许我们根本不需要减 mean，只控制 vector 的整体大小就够了。**

---

# 15. LayerNorm vs RMSNorm

## LayerNorm

```text
x
↓
减 mean
↓
除 standard deviation
↓
learnable scale/shift
```

它做两件核心事情：

```text
Re-centering
+
Re-scaling
```

---

## RMSNorm

```text
x
↓
计算 RMS
↓
除 RMS
↓
learnable scale
```

它主要做：Re-scaling，而不做：Re-centering

所以 slide 写：**Only rescaling, no recentering**

---

# 16. RMS 是什么？

RMS： Root Mean Square

公式：

$$
RMS(x)=
\sqrt{
\frac{1}{d}
\sum_i x_i^2
+\epsilon
}
$$

大白话：

```text
每个数字平方
↓
求平均
↓
开平方
```

它在衡量： **这个 vector 整体有多“大”。**

---

# 17. 一个 RMSNorm 小例子

还是：

```text
x = [2,4,6,8]
```

先平方：

```text
[4,16,36,64]
```

求平均：

$$
\frac{4+16+36+64}{4}=30
$$

开平方：

$$
RMS(x)=\sqrt{30}\approx5.48
$$

然后：

```text
[2,4,6,8] / 5.48

≈

[0.37,0.73,1.10,1.46]
```

注意：

LayerNorm 得到的例子大约是：

```text
[-1.34,-0.45,0.45,1.34]
```

RMSNorm：

```text
[0.37,0.73,1.10,1.46]
```

为什么完全不一样？

因为：

```text
LayerNorm
减掉了 mean = 5

RMSNorm
根本没有减 mean
```

---

# 18. RMSNorm 为什么可能够用？

LayerNorm 的目标是帮助 optimization。

RMSNorm 的研究发现： **很多情况下，真正非常重要的是控制 representation 的 scale；re-centering 可能不是必需的。**

于是：

```text
LayerNorm：
控制 center + scale

RMSNorm：
主要控制 scale
```

少做一步，architecture 更简单。而实验发现它仍然可以获得很好的 training behavior。

---

# 19. 用“音量”理解 RMSNorm

假设一个 token 的 hidden representation 是一段音乐。

LayerNorm：

```text
① 调整中心位置
② 调整整体音量
```

RMSNorm：

```text
只管一件事：

“你声音太大了，
先把整体音量控制到合理范围。”
```

它不太在乎：你的平均值是不是刚好在 0

只关心：整体 scale 不要失控


---

# 20. 为什么“更简单”可能反而很好？

大模型会把同一个 operation 执行：

```text
很多 layers
×
很多 tokens
×
很多 batches
×
很多 training steps
```

所以一个很小的 simplification：

```text
少一次 mean calculation
少一次 recentering
```

乘上巨大的训练规模以后，也可能产生实际的效率收益。

更重要的是：**如果简化以后 training quality 仍然很好，那复杂步骤就没有必要为了复杂而保留。**

这正是最后一张 slide： **Simplicity is often the winner.**

想表达的 architecture philosophy。

---

# 21. LayerNorm 和 RMSNorm 不是“模型学习知识”的主要地方

非常容易混淆。

```text
Attention
→ 从其他 tokens 获取 contextual information

FFN
→ nonlinear feature transformation

LayerNorm / RMSNorm
→ 控制 representation 的 numerical scale / stability
```

所以：

```text
Attention / FFN ≈ 干活的人

Norm ≈ 保证工作环境稳定的人
```

---

# 22. 把这一节和上一节 Efficient Attention 连起来

最近 Transformer architecture 的很多改进，并不是：“重新发明 Transformer。”

而是在原始 architecture 的几个地方做 targeted optimization：

```text
Feed Forward
↓
GELU / SwiGLU / gated FFN

Attention
↓
GQA / MQA / MLA
减少 KV Cache

Normalization
↓
Post-LN → Pre-LN
LayerNorm → RMSNorm

Position
↓
新的 positional encoding 方法
```

核心目标通常是：

```text
更容易训练
更稳定
更省显存
更快 inference
更容易 scale 到大模型
```

---

# 23. 为什么 slide 最后说 Most upgrades are for efficiency and stability？

因为原始 Transformer 的核心思想：

```text
Attention
+
FFN
+
Residual
+
Normalization
```

本身已经非常强。

后来模型从：几亿 parameters

变成：

```text
几十亿
几百亿
更多
```

context 也越来越长。

问题逐渐变成：

```text
怎么训练得动？
怎么让 gradient 不爆？
怎么少占显存？
怎么 generation 更快？
```

所以 architecture research 的很多工作是在： **保留 Transformer 强大的核心能力，同时把昂贵、不稳定、冗余的部分变得更简单。**

---

# 24. 一张图记住 Normalization 演进

```text
Original Transformer

Attention / FFN
      ↓
Residual Add
      ↓
LayerNorm

= Post-LN


        ↓
为了深层训练更稳定


LayerNorm
   ↓
Attention / FFN
   ↓
Residual Add

= Pre-LN


        ↓
进一步简化 normalization


RMSNorm
   ↓
Attention / FFN
   ↓
Residual Add

= Pre-Norm + simpler normalization
```

---

# 25. LayerNorm vs RMSNorm 表格

| | LayerNorm | RMSNorm |
|---|---|---|
| subtract mean | Yes | No |
| normalize scale | Yes | Yes |
| recenter | Yes | No |
| rescale | Yes | Yes |
| learnable scale | Yes | Yes |
| conceptual complexity | higher | simpler |
| purpose | training stability | training stability with simpler normalization |

不要把表理解成：RMSNorm 永远比 LayerNorm 好

而应该理解成：**RMSNorm 是一个更简单、在很多 Transformer/LLM 设置中效果很好的 normalization choice。**

---

# 26. Pre-LN vs Post-LN 表格

| | Post-LN | Pre-LN |
|---|---|---|
| Norm location | after residual/sublayer | before sublayer |
| Formula | $LN(x+F(x))$ | $x+F(LN(x))$ |
| Original Transformer | Yes | No |
| Deep training | can be more sensitive | often more stable |
| Residual gradient path | less direct through stack | cleaner identity path |

---

# 27. 和你的 DecoderBlock 完全对应

你的代码：

```python
ln_x = self.ln_1(x)

mha = self.mha(
    ln_x,
    attention_mask
)

x = x + mha
```

就是：

$$
x
\leftarrow
x+MHA(LN(x))
$$

然后：

```python
ln_x = self.ln_2(x)

ff = self.ff(ln_x)

x = x + ff
```

就是：

$$
x
\leftarrow
x+FFN(LN(x))
$$

所以你的 DecoderBlock：

```text
Pre-LN
+
Residual Connections
+
MHA
+
FFN
```

这不是新的抽象知识。

> **你之前已经实现过 Pre-LN，只是当时还没有给这个 architecture pattern 起名字。**

---

# 28. 如果把 LayerNorm 换成 RMSNorm 呢？

概念上结构几乎不用改：

原来：

```python
self.ln_1 = nn.LayerNorm(n_embd)
self.ln_2 = nn.LayerNorm(n_embd)
```

可以想象变成：

```text
self.norm_1 = RMSNorm(...)
self.norm_2 = RMSNorm(...)
```

DecoderBlock 仍然：

```text
Norm
↓
Attention
↓
Residual

Norm
↓
FFN
↓
Residual
```

只是：

```text
Norm 内部怎么 normalize
```

发生了变化。

---

# 29. 面试：What does LayerNorm do?

> **LayerNorm normalizes each token's hidden representation across its feature dimension, helping keep activation scales well behaved and improving optimization stability. It then applies learnable affine parameters.**

---

# 30. 面试：Pre-LN vs Post-LN?

> **Post-LN applies normalization after the residual addition, while Pre-LN normalizes the input before the attention or feed-forward sublayer. Pre-LN provides a cleaner residual path and is commonly used to improve optimization stability in deep Transformers.**

---

# 31. 面试：What is RMSNorm?

> **RMSNorm is a simplified normalization method that rescales a hidden vector using its root-mean-square magnitude without subtracting the mean. It removes the recentering step of LayerNorm while retaining scale normalization.**

---

# 32. 最终只背这五句话

如果考试前只有一分钟：

```text
1. LayerNorm  = 稳定每个 token hidden vector 的数值尺度。

2. Post-LN = 干完活 + residual 后再 Norm。

3. Pre-LN  = 先 Norm，再让 Attention/FFN 干活，
     residual path 更直接，深层训练通常更稳定。

4. RMSNorm  = LayerNorm 的简化版：
     不减 mean，只控制整体 scale。

5. Modern Transformer improvements
   很多不是改变“模型会不会思考”，
   而是让模型更稳定、更高效、更容易 scale。
```

最核心的 mental model： **Attention/FFN 负责“学和处理信息”；LayerNorm/RMSNorm 负责“别让整个系统在几十上百层计算中数值失控”。**
