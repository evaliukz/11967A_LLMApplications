# Transformer Feed-Forward Network (FFN) 详细笔记

## 1. Feed Forward 是什么？

Transformer Decoder Block 里有两个非常重要的计算：

``` text
Masked Self-Attention
→ Add & Norm
→ Feed-Forward Network (FFN)
→ Add & Norm
```

最重要的区别：

> **Self-Attention 负责让不同 token 之间交换信息；Feed Forward负责对每一个 token 自己的 representation 做非线性加工。**

英文记忆：

> **Attention mixes information across tokens; the FFN transforms
> information within each token.**

可以把它类比成：

``` text
Attention = 大家开会，互相交流
FFN       = 开完会后，每个人独立思考
```

------------------------------------------------------------------------

## 2. 作业里的 FFN 结构

``` python
middle_dim = 4 * n_embd
self.linear_in = nn.Linear(n_embd, middle_dim)
self.linear_out = nn.Linear(middle_dim, n_embd)
self.dropout = nn.Dropout(p_dropout)
```

令：D = n_embd

那么：middle\_dim = 4D

整个 FFN：

``` text
D
↓ Linear
4D
↓ GeLU
4D
↓ Linear
D
↓ Dropout
D
```

数学公式：

$$
FFN(x)=
Dropout
\left(
GeLU(xW_1+b_1)W_2+b_2
\right)
$$

------------------------------------------------------------------------

## 3. 输入 `x`

输入：x.shape = [B,S,D]

其中：

``` text
B = batch size
S = sequence length
D = hidden dimension = n_embd
```

例如：

``` text
B = 2
S = 5
D = 768
```

那么：x.shape = [2,5,768]

意思是 2 个 sequences，每个有 5 个 token，每个 token 当前用一个 768 维vector 表示。

------------------------------------------------------------------------

## 4. FFN 是 Position-wise 的

假设 sequence：I love cats

Attention 后：

``` text
I    → h1
love → h2
cats → h3
```

FFN 分别计算：

$$
FFN(h_1)
$$

$$
FFN(h_2)
$$

$$
FFN(h_3)
$$

每个 token 独立经过同一套 FFN parameters。

所以它叫：**Position-wise Feed-Forward Network**

FFN 本身不会重新让 `I`、`love`、`cats` 互相做 attention。

------------------------------------------------------------------------

## 5. Step 1：Linear In，把 D 扩展成 4D

``` python
self.linear_in = nn.Linear(D, 4 * D)
```

所以：

``` text
[B,S,D] → [B,S,4D]
```

例如：

``` text
[2,5,768]
→
[2,5,3072]
```

数学上，对每个 token：

$$
h=xW_1+b_1
$$

其中：

$$
W_1 \in \mathbb{R}^{D \times 4D}
$$

因此：

$$
\mathbb{R}^{D}
\rightarrow
\mathbb{R}^{4D}
$$

`nn.Linear` 只处理 tensor 最后一维，因此 `B` 和 `S` 不变。

------------------------------------------------------------------------

## 6. 为什么要从 D 扩展到 4D？

可以理解成：

> **先把 token 的 representation
> 展开到一个更大的"思考空间"，进行更丰富的 feature
> transformation，再压回原来的 D 维。**

例如：D=4, 则4D=16

所以：4 dimensions→16 dimensions→4 dimensions

这里不是简单把原来的数字复制四遍。`W1` 是 learned parameters，模型在学习如何把原来的 representation 转换成更丰富的intermediate features。

GPT-2 经典设计使用：

$$
D \rightarrow 4D \rightarrow D
$$

但 `4D` 是 architecture choice，并不是所有 Transformer 都必须严格使用 4
倍。

------------------------------------------------------------------------

## 7. Step 2：GeLU

作业使用 GPT-2 风格的 GeLU：

``` python
y = F.gelu(self.linear_in(x))
```

shape：

``` text
[B,S,D]
↓ Linear
[B,S,4D]
↓ GeLU
[B,S,4D]
```

GeLU 不改变 shape，只改变里面的数值。

GeLU 全称： **Gaussian Error Linear Unit**

经典定义：

$$
GeLU(x)=x\Phi(x)
$$

其中：

$$
\Phi(x)
$$

是标准高斯分布的 cumulative distribution function。

------------------------------------------------------------------------

## 8. 为什么需要 GeLU？

如果没有 activation，只有两个 Linear Layers：

$$
xW_1W_2
$$

可以把：

$$
W_1W_2
$$

合并成另一个矩阵：

$$
W=W_1W_2
$$

于是：

$$
xW_1W_2=xW
$$

本质上还是一个 linear transformation。

加入 GeLU 后：

$$
GeLU(xW_1+b_1)
$$

网络获得 non-linearity，因此可以学习复杂得多的 feature transformations。

一句话：**GeLU introduces nonlinearity into the FFN.**

------------------------------------------------------------------------

## 9. Step 3：Linear Out，把 4D 压回 D

``` python
self.linear_out = nn.Linear(4 * D, D)
```

shape：

``` text
[B,S,4D]
→
[B,S,D]
```

例如：

``` text
[2,5,3072]
→
[2,5,768]
```

数学上：

$$
z=yW_2+b_2
$$

其中：

$$
W_2 \in \mathbb{R}^{4D \times D}
$$

所以：

$$
\mathbb{R}^{4D}
\rightarrow
\mathbb{R}^{D}
$$

------------------------------------------------------------------------

## 10. 为什么一定要回到 D？

Transformer block 需要继续做 residual connection，例如：

``` python
x + ffn_output
```

如果：

``` text
x.shape = [B,S,D]
```

那么 `ffn_output` 也必须是：[B,S,D] 才能 element-wise 相加。

所以 FFN 最终必须回到原来的 hidden dimension：

$$
D \rightarrow 4D \rightarrow D
$$

------------------------------------------------------------------------

## 11. Step 4：Dropout

最后：

``` python
z = self.dropout(self.linear_out(y))
```

Dropout 是 regularization。训练时随机 drop 一部分 activations，减少模型过度依赖某些 features。

它不改变 shape：

``` text
[B,S,D]
→
[B,S,D]
```

------------------------------------------------------------------------

## 12. 作业 `forward()` 的计算逻辑

starter code：

``` python
y = F.gelu(...)
z = self.dropout(...)
return z
```

对应：

``` python
def forward(self, x: torch.FloatTensor) -> torch.FloatTensor:
    y = F.gelu(self.linear_in(x))
    z = self.dropout(self.linear_out(y))
    return z
```

第一行：

``` text
D → 4D → GeLU
```

第二行：

``` text
4D → D → Dropout
```

------------------------------------------------------------------------

## 13. 完整 Shape Flow

``` text
x
[B,S,D]

↓ linear_in

[B,S,4D]

↓ GeLU

[B,S,4D]

↓ linear_out

[B,S,D]

↓ Dropout

z
[B,S,D]
```

------------------------------------------------------------------------

## 14. 一个具体例子

假设：

``` text
B = 1
S = 3
D = 4
```

sequence：

``` text
I love cats
```

输入：

``` text
x.shape = [1,3,4]
```

每个 token 有一个 4 维 vector：

``` text
I    → [0.2, 0.5, -0.3, 0.8]
love → [0.7, -0.1, 0.4, 0.2]
cats → [-0.2, 0.9, 0.6, 0.1]
```

因为：

$$
middle\_dim=4D=16
$$

所以：

``` text
Step 1: Linear In
[1,3,4] → [1,3,16]

Step 2: GeLU
[1,3,16] → [1,3,16]

Step 3: Linear Out
[1,3,16] → [1,3,4]

Step 4: Dropout
[1,3,4] → [1,3,4]
```

最终每个 token 又回到 4-dimensional hidden representation。

------------------------------------------------------------------------

## 15. Attention 和 FFN 到底有什么区别？

### Self-Attention

负责： **不同 token 之间的信息交流。**

也就是：mix information across token positions

### Feed Forward

负责： **对每个 token 已经获得的 contextual representation 做 nonlineartransformation。**

也就是：transform each token independently

面试可以说：

> **Self-attention mixes information across token positions, while the
> feed-forward network independently transforms each token
> representation.**

------------------------------------------------------------------------

## 16. 大白话类比

假设三个 token 是三个人：

``` text
Alice
Bob
Charlie
```

Attention 相当于三个人开会，相互获取信息。

FFN 相当于会议结束后，每个人回到自己的房间，独立加工刚才获得的信息。

三个人使用相同的"思考程序"（same FFN parameters），但是各自独立计算。

所以：

``` text
Attention = communication
FFN       = independent processing
```

------------------------------------------------------------------------

## 17. 为什么 Transformer 两个都需要？

可以理解成：

``` text
Attention = Gather / Mix Information

↓

FFN = Process / Transform Information
```

一句话：

> **Attention decides what information to gather; the FFN processes the gathered information.**

------------------------------------------------------------------------

## 18. 和整个 Decoder Block 连起来

``` text
x
[B,S,D]

↓ Masked Multi-Head Self-Attention

[B,S,D]

↓ Residual / Add & Norm

[B,S,D]

↓ Feed Forward

Linear(D,4D)

↓ GeLU

Linear(4D,D)

↓ Dropout

[B,S,D]

↓ Residual / Add & Norm

[B,S,D]

↓ Next Decoder Block
```

------------------------------------------------------------------------

## 19. 一个容易误解的地方

作业 docstring 写：

``` text
The feedforward attention module in a decoder block.
```

严格来说：

> **Feed-Forward Network 不是 Attention。**

`Multi-Head Self-Attention` 和 `Feed-Forward Network` 是 Transformer
block 中两个不同的 sublayers。

------------------------------------------------------------------------

## 20. 面试：What is the Feed-Forward Network in a Transformer?

> **The feed-forward network is a small MLP applied independently to
> each token position. It projects each token from the hidden dimension
> into a larger intermediate dimension, applies a nonlinear activation,
> and projects it back to the original hidden dimension.**

可以补一句：

> **Self-attention mixes information across tokens, while the
> feed-forward network independently transforms each token's
> representation.**

------------------------------------------------------------------------

## 21. 面试：Why expand the hidden dimension?

> **The larger intermediate dimension gives the network more capacity to
> learn nonlinear feature transformations. The representation is then
> projected back to the model hidden dimension so it can continue
> through residual connections and subsequent Transformer blocks.**

------------------------------------------------------------------------

## 22. 面试：Why do we need GeLU?

> **GeLU introduces nonlinearity between the two linear layers. Without
> an activation function, multiple linear layers could collapse into a
> single linear transformation and would have much less expressive
> power.**

------------------------------------------------------------------------

## 23. 最终心智模型

看到：

``` python
y = F.gelu(self.linear_in(x))
z = self.dropout(self.linear_out(y))
```

脑子里直接翻译：

``` text
x [B,S,D]

↓ Linear(D,4D)
把每个 token 展开到更大的 representation space

↓ GeLU
加入 non-linearity

↓ Linear(4D,D)
把每个 token 压回原来的 hidden dimension

↓ Dropout
regularization

↓
z [B,S,D]
```

最重要的一句话：

> **Attention 负责让 tokens 互相交流；Feed Forward 负责让每个 token
> 对交流后的信息独立进行更深的非线性加工。**

英文：

> **Attention mixes information across tokens; the FFN transforms that
> information within each token.**


# Transformer Decoder Block：Feed Forward、LayerNorm 与 Residual Connection

## 1. 先解决最关键的误解：这里没有“两个 Feed Forward”

代码里：

```python
self.ln_1 = nn.LayerNorm(n_embd)
self.mha = MultiHeadAttention(n_embd, n_head)
self.ff = FeedForward(n_embd)
self.ln_2 = nn.LayerNorm(n_embd)
```

实际上只有一个 FeedForward module：`self.ff`。`ln_1` 和 `ln_2` 是两个 LayerNorm modules。

先记：

```text
LayerNorm  = 整理 / 稳定输入数值
Attention  = 让 tokens 互相交换信息
FeedForward = 每个 token 独立进行 nonlinear processing
Residual   = 保留原信息 + 加上新学到的信息
```

## 2. FeedForward 和 LayerNorm 的区别

FFN 是真正学习 feature transformation 的小型 MLP：

```text
D → Linear → 4D → GeLU → Linear → D
```

公式：

$$
FFN(x)=W_2GeLU(W_1x+b_1)+b_2
$$

它的任务是改变、加工 token representation。

LayerNorm 主要把一个 token 的 hidden vector 调整到更稳定的数值尺度：

$$
LayerNorm(x)=\gamma\frac{x-\mu}{\sqrt{\sigma^2+\epsilon}}+\beta
$$

其中：

$$
\mu=\frac{1}{D}\sum_{i=1}^{D}x_i
$$

$$
\sigma^2=\frac{1}{D}\sum_{i=1}^{D}(x_i-\mu)^2
$$

一句话：

> **LayerNorm stabilizes the representation; FeedForward transforms the representation.**

它们不是二选一，而是在解决两个不同的问题。

## 3. DecoderBlock 的 Pre-LN 结构

你的代码属于 Pre-LN 风格：LayerNorm 放在 Attention / FFN 之前。

标准 Pre-LN 可以写成：

$$
x_1=x+MHA(LN_1(x))
$$

然后：

$$
y=x_1+FFN(LN_2(x_1))
$$

结构：

```text
x
│
├──────── residual ────────┐
↓                          │
LayerNorm 1                │
↓                          │
Masked MHA                 │
↓                          │
+ ←────────────────────────┘
│
x1
│
├──────── residual ────────┐
↓                          │
LayerNorm 2                │
↓                          │
Feed Forward               │
↓                          │
+ ←────────────────────────┘
│
y
```

## 4. 你贴出的最后一行需要特别注意

当前代码是：

```python
ln_x = self.ln_2(x)
return self.ff(ln_x)
```

这意味着：

$$
y=FFN(LN_2(x_1))
$$

这里没有第二个 residual addition。

标准 Pre-LN Transformer block 通常是：

```python
ln_x = self.ln_2(x)
ff = self.ff(ln_x)
y = x + ff
return y
```

即：

$$
y=x_1+FFN(LN_2(x_1))
$$

如果这是尚未完成的 starter code，第二个 residual connection 很可能是需要补充的逻辑；最终以课程 specification/tests 为准。

## 5. 为什么 Attention 前需要 `ln_1`？

```python
ln_x = self.ln_1(x)
mha = self.mha(ln_x, attention_mask)
x = x + mha
```

数学上：

$$
x_1=x+MHA(LN_1(x))
$$

LayerNorm 先让每个 token 的 hidden representation 处在较稳定的 scale，然后 Attention 再从这些 normalized representations 生成 Q、K、V 并交换信息。

## 6. 为什么 FFN 前又需要 `ln_2`？

Attention 已经改变了 representation：

$$
x_1=x+MHA(LN_1(x))
$$

所以 `x_1` 和最初的 `x` 已经不同。FFN 前再次做：

$$
LN_2(x_1)
$$

为 FFN 准备稳定输入。

因此：

```text
LN1 → 服务于 Attention
LN2 → 服务于 FFN
```

## 7. 一个很小的 LayerNorm 数值例子

假设某个 token：

$$
x=[2,4,6,8]
$$

且：

$$
D=4
$$

平均值：

$$
\mu=\frac{2+4+6+8}{4}=5
$$

variance：

$$
\sigma^2=\frac{(2-5)^2+(4-5)^2+(6-5)^2+(8-5)^2}{4}=5
$$

所以：

$$
\sigma=\sqrt{5}\approx2.236
$$

暂时忽略 $\gamma,\beta,\epsilon$，LayerNorm 后约为：

$$
[-1.34,-0.45,0.45,1.34]
$$

注意 shape 没变：

```text
[D] → [D]
```

LayerNorm 主要调整 numerical scale，而不是把 4 维变成 16 维。

## 8. FFN 对 normalized vector 做什么？

如果：

$$
D=4
$$

则：

$$
4D=16
$$

FFN：

```text
4 → 16 → GeLU → 4
```

假设 FFN 最终输出：

$$
[0.3,-0.2,0.5,0.1]
$$

可以把它理解成 FFN 对当前 token 建议的一个 learned update。

## 9. Residual Connection 在干什么？

假设 Attention 后 hidden state：

$$
x_1=[2.5,3.8,6.2,7.5]
$$

FFN 输出：

$$
FFN(LN(x_1))=[0.3,-0.2,0.5,0.1]
$$

标准 Pre-LN residual：

$$
y=x_1+FFN(LN(x_1))
$$

所以：

$$
y=[2.8,3.6,6.7,7.6]
$$

最好的心智模型：

```text
原来的 representation
+
这一层学到的 update
=
新的 representation
```

## 10. 一个 token 完整走过 Decoder Block

假设：

$$
x=[2,4,6,8]
$$

### Step 1 — LayerNorm 1

$$
LN_1(x)\approx[-1.34,-0.45,0.45,1.34]
$$

目的：稳定输入尺度。

### Step 2 — Multi-Head Attention

Attention 实际处理整个 sequence。假设它综合 context 后为当前 token 输出：

$$
MHA(LN_1(x))=[0.5,-0.2,0.2,-0.5]
$$

这是 contextual update。

### Step 3 — 第一个 Residual

$$
x_1=x+MHA(LN_1(x))
$$

$$
x_1=[2.5,3.8,6.2,7.5]
$$

### Step 4 — LayerNorm 2

再次 normalize 更新后的 representation。假设得到：

$$
LN_2(x_1)\approx[-1.22,-0.57,0.62,1.17]
$$

### Step 5 — Feed Forward

```text
4 → 16 → GeLU → 4
```

假设输出：

$$
[0.3,-0.2,0.5,0.1]
$$

### Step 6 — 第二个 Residual

$$
y=x_1+FFN(LN_2(x_1))
$$

$$
y=[2.8,3.6,6.7,7.6]
$$

这就是一个 Decoder Block 对 representation 做的核心事情。

## 11. 用故事理解整个 Decoder Block

```text
x
= 我目前知道的信息

LayerNorm 1
= 先把信息整理到稳定尺度

Attention
= 去听其他 tokens 在说什么

Residual
= 保留原来的我 + 加入交流获得的信息

LayerNorm 2
= 把更新后的信息再整理一下

Feed Forward
= 每个 token 自己独立思考和加工

Residual
= 保留刚才的我 + 加上新的思考结果
```

## 12. 为什么四种组件都需要？

```text
LayerNorm = stabilize
Attention = communicate
FFN       = transform
Residual  = preserve + update
```

LayerNorm 解决 numerical stability / optimization。

Attention 解决 token-to-token information mixing。

FFN 解决 nonlinear feature transformation。

Residual 让模型保留已有 representation，并逐层添加 update，同时为深层网络提供更直接的 gradient path。

## 13. Pre-LN vs Post-LN

### Pre-LN

LayerNorm 在 sublayer 之前：

$$
x_1=x+Sublayer(LN(x))
$$

也就是：

```text
LN → Sublayer → Residual Add
```

### Post-LN

原始 2017 Transformer 使用经典 Post-LN：

$$
x_1=LN(x+Sublayer(x))
$$

也就是：

```text
Sublayer → Residual Add → LN
```

记忆：

```text
Pre-LN  : LN → Sublayer → Add
Post-LN : Sublayer → Add → LN
```

现代深层 Transformer / LLM 中 Pre-LN 风格非常常见，因为通常更容易优化深层网络。

## 14. 为什么 FeedForward class 自己没有 LayerNorm？

这是代码设计上的 separation of responsibilities。

```text
FeedForward
= 只负责 D → 4D → GeLU → D

DecoderBlock
= 决定 LayerNorm、Attention、FFN、Residual 如何组合
```

这样 `FeedForward` 不需要知道外部 architecture 使用 Pre-LN 还是 Post-LN。

可以理解为：

```text
FeedForward       = 一个零件
MultiHeadAttention = 一个零件
LayerNorm         = 一个零件
DecoderBlock      = 把零件按照 Transformer 架构组装起来
```

## 15. Shape Flow

Decoder Block 外部：

```text
x [B,S,D]
```

LayerNorm：

```text
[B,S,D] → [B,S,D]
```

MHA：

```text
[B,S,D] → [B,S,D]
```

Residual：

```text
[B,S,D] + [B,S,D] → [B,S,D]
```

FFN 内部：

```text
[B,S,D]
→ [B,S,4D]
→ GeLU
→ [B,S,D]
```

所以整个 Decoder Block：

$$
[B,S,D]\rightarrow[B,S,D]
$$

这使多个 blocks 可以不断 stack。

## 16. 完整 Pre-LN Decoder Block 代码心智模型

```python
def forward(self, x, attention_mask):
    # Attention sublayer
    ln_x = self.ln_1(x)
    mha = self.mha(ln_x, attention_mask)
    x = x + mha

    # Feed-forward sublayer
    ln_x = self.ln_2(x)
    ff = self.ff(ln_x)
    x = x + ff

    return x
```

公式：

$$
x_1=x+MHA(LN_1(x))
$$

$$
y=x_1+FFN(LN_2(x_1))
$$

## 17. 和 QKV / Attention 串起来

```text
Input x [B,S,D]

↓ LayerNorm 1
[B,S,D]

↓ QKV Projection
Q  = [B,H,S,HD]
Kᵀ = [B,H,HD,S]
V  = [B,H,S,HD]

↓ QKᵀ / sqrt(HD)
Attention logits [B,H,S,S]

↓ Causal Mask + Padding Mask
↓ Softmax
Attention weights [B,H,S,S]

↓ Attention weights × V
[B,H,S,HD]

↓ Merge Heads
MHA output [B,S,D]

↓ Residual
x + MHA(...)
[B,S,D]

↓ LayerNorm 2
[B,S,D]

↓ Feed Forward
[B,S,D] → [B,S,4D] → GeLU → [B,S,D]

↓ Residual
[B,S,D]

↓ Next Decoder Block
```

## 18. 面试快速回答

### Why do Transformers need both LayerNorm and FFN?

> **They serve different purposes. LayerNorm stabilizes the scale of hidden representations and helps optimization, while the feed-forward network learns nonlinear feature transformations for each token independently.**

### Why are there two LayerNorms in a Transformer block?

> **A Transformer block has two major sublayers: attention and the feed-forward network. In a Pre-LN architecture, each sublayer receives its own normalized input, so one LayerNorm is used before attention and another before the FFN.**

### What is a residual connection?

> **A residual connection adds the sublayer's output back to its input. This preserves existing information and provides a direct path for gradients through deep Transformer networks.**

数学上：

$$
y=x+F(x)
$$

可以把 $F(x)$ 理解成这一层学习的 update。

## 19. 最终心智模型

看到：

```python
ln_x = self.ln_1(x)
mha = self.mha(ln_x, attention_mask)
x = x + mha

ln_x = self.ln_2(x)
ff = self.ff(ln_x)
x = x + ff
```

脑子里翻译：

```text
LN1      = 先整理输入
MHA      = 和其他 tokens 交流
Residual = 原信息 + 交流获得的新信息
LN2      = 再整理更新后的 representation
FFN      = 每个 token 独立做 nonlinear processing
Residual = 保留已有信息 + 加上新的加工结果
```

最终公式：

$$
x_1=x+MHA(LN_1(x))
$$

$$
y=x_1+FFN(LN_2(x_1))
$$

一句话总结：

> **LayerNorm 负责稳定，Attention 负责交流，Feed Forward 负责加工，Residual 负责保留旧信息并叠加新信息。**


# Transformer Residual Connection：为什么 `x + mha` 才叫 Residual？

## 1. 最重要的结论

**Multi-Head Attention 的结果本身不叫 Residual Connection。**

代码：

```python
mha = self.mha(ln_x, attention_mask)
x = x + mha  # Residual connection
```

这里实际上有两个步骤：

```text
mha = Attention 的输出

x + mha = Residual Connection
```

所以注释：

```python
# Residual connection
```

指的是：x = x + mha 这个**加法操作**，而不是 `mha` 本身。

---

## 2. `mha` 是什么？

代码：

```python
mha = self.mha(ln_x, attention_mask)
```

Multi-Head Attention 内部进行了：

$$
Attention(Q,K,V)=
softmax
\left(
\frac{QK^T}{\sqrt{HD}}
\right)V
$$

最终：

```text
mha.shape = [B,S,D]
```

`mha` 表示： 每个 token 通过 Attention 从自己和其他允许访问的 tokens 中收集到的 contextual information。

因此：

```text
mha = Attention 计算出来的新信息 / contextual update
```

它本身还不是 Residual Connection。

---

## 3. 什么才是 Residual Connection？

下一行：

```python
x = x + mha
```

这一步才叫：**Residual Connection / Skip Connection**

数学上：

$$
x_{new}=
x_{old}
+
MHA(LN(x_{old}))
$$

可以理解成：

```text
新的 representation =
原来的 representation
+
Attention 学到的新信息
```

---

## 4. 一个实际的小例子

假设某个 token 原来的 hidden representation：

$$
x_{old}=
[2,4,6,8]
$$

经过 LayerNorm 和 Multi-Head Attention 后，Attention 输出：

$$
mha=
[0.5,-0.2,0.2,-0.5]
$$

这里：

```text
mha
```

可以理解成 Attention 建议加入的 contextual update。

然后执行：

```python
x = x + mha
```

也就是：

$$
x_{new}=
[2,4,6,8]
+
[0.5,-0.2,0.2,-0.5]
$$

得到：

$$
x_{new}=
[2.5,3.8,6.2,7.5]
$$

所以：

```text
原来的信息
+
Attention 新学到的信息
=
更新后的 representation
```

---

## 5. 为什么叫 Skip Connection？

数据流可以画成：

```text
原来的 x
     │
     ├──────────────────────────────┐
     │                              │
     ↓                              │
 LayerNorm                          │
     ↓                              │
Multi-Head Attention                │
     ↓                              │
    mha                             │
     │                              │
     └────────────── + ←────────────┘
                    ↓
                  new x
```

原始的：

```text
x
```

有一条路径直接**绕过（skip）** LayerNorm 和 Attention，到最后参与加法。

所以也叫：

> **Skip Connection**

---

## 6. 为什么叫 Residual？

假设一个 neural network layer 学习：

$$
F(x)
$$

普通网络可能直接：

$$
y=F(x)
$$

Residual Network 则使用：

$$
y=x+F(x)
$$

也就是说，模型不需要每一层都重新创造一个完整 representation。

它只需要学习：

> **在原来的 `x` 基础上，我应该增加或者修改什么？**

这里的：

$$
F(x)
$$

可以理解成一个 update / residual。

对于 Transformer Attention：

$$
F(x)=
MHA(LN(x))
$$

所以：

$$
y=
x
+
MHA(LN(x))
$$

---

## 7. 大白话理解

可以把：

```text
x
```

理解成：

> 我目前已经知道的信息。

把：

```text
mha
```

理解成：

> Attention 根据上下文告诉我应该补充的信息。

于是：

```text
x + mha
```

就是：

> 保留原来的知识，再加入 Attention 得到的新 context。

所以：

```text
x
=
原来的信息

mha
=
Attention 新学到的信息

x + mha
=
原来的信息 + 新信息
```

---

## 8. FFN 后面也有 Residual Connection

Residual Connection 并不是 Attention 独有的。

Feed Forward 后面通常也有：

$$
y
=
x
+
FFN(LN(x))
$$

所以标准 Pre-LN Decoder Block 有两个 residual connections。

---

## 9. Attention 部分

$$
x_1
=
x
+
MHA(LN_1(x))
$$

结构：

```text
x
│
├─────────────────────┐
↓                     │
LayerNorm             │
↓                     │
Attention             │
↓                     │
MHA output            │
│                     │
└──────── + ←─────────┘
          ↓
          x₁
```

这里：

```text
MHA output
```

是新信息。

而：

```text
x + MHA output
```

才是 Residual Connection。

---

## 10. Feed Forward 部分

接下来：

$$
y
=
x_1
+
FFN(LN_2(x_1))
$$

结构：

```text
x₁
│
├─────────────────────┐
↓                     │
LayerNorm             │
↓                     │
Feed Forward          │
↓                     │
FFN output            │
│                     │
└──────── + ←─────────┘
          ↓
          y
```

这里：

```text
FFN output
```

是 FFN 学到的 update。

而：

```text
x₁ + FFN output
```

才是第二个 Residual Connection。

---

## 11. 一个完整 Pre-LN Decoder Block

代码可以理解成：

```python
# Attention
ln_x = self.ln_1(x)
mha = self.mha(ln_x, attention_mask)
x = x + mha

# Feed Forward
ln_x = self.ln_2(x)
ff = self.ff(ln_x)
x = x + ff

return x
```

对应：

$$
x_1=
x
+
MHA(LN_1(x))
$$

然后：

$$
y=
x_1
+
FFN(LN_2(x_1))
$$

---

## 12. 最终心智模型

整个 Decoder Block 可以记成：

```text
原来的 x
     ↓
LayerNorm
     ↓
Attention
     ↓
Attention 学到的新信息
     ↓
+ 原来的 x
     ↓
更新后的 x₁
     ↓
LayerNorm
     ↓
Feed Forward
     ↓
FFN 学到的新信息
     ↓
+ 原来的 x₁
     ↓
最终 y
```

最重要的区别：

```text
mha
=
Attention Output

x + mha
=
Residual Connection
```

同理：

```text
ff = Feed Forward Output

x + ff = Residual Connection
```

---

# 13. 一句话记忆

> **Attention output 是“新学到的信息”，Residual Connection 是“原来的信息 + 新学到的信息”。**

数学上最值得记住：

$$
\boxed{y=x+F(x)}
$$

其中：

$$
F(x)
$$

可以是：

```text
Multi-Head Attention
```

也可以是：

```text
Feed-Forward Network
```

---

# 14. 面试回答

如果面试官问：

> **What is a residual connection in a Transformer?**

可以回答：

> **A residual connection adds a sublayer's output back to its original input. For example, after self-attention, the Transformer computes `x + attention(x)`. This preserves the existing representation while allowing the sublayer to learn an update to it.**

最短记忆：

> **Residual = original representation + learned update.**
