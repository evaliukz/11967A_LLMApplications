# Scaling Up Pre-training Data

## 1. 整节课的主线

高质量数据（Wikipedia、books、curated datasets）很好，但数量有限，而现代LLM 非常 **data-hungry**。

``` text
高质量数据不够
    ↓
去 Internet 找海量数据
    ↓
Web 数据很多，但很脏
    ↓
Crawler 抓取一部分网页
    ↓
Cleaning + Filtering + Deduplication
    ↓
Tokenization + Batching
    ↓
LLM Pre-training
```

核心结论： **大规模预训练不仅是模型问题，也是一个大规模数据系统与 dataset design 问题。**

## 2. More Data is Almost Always Better

课件中的 RoBERTa 实验显示，随着数据规模和训练量增加，MNLI 和 SQuAD表现持续改善。

  Setup                   Data   Steps   MNLI-m   SQuAD1.1
  -------------------- ------- ------- -------- ----------
  Wiki + Books            16GB    100K     89.0       93.6
  \+ Additional Data     160GB    100K     89.3       94.0
  \+ More Steps          160GB    300K     90.0       94.4
  \+ Even More Steps     160GB    500K     90.2       94.6

大白话：

> **模型很能"吃数据"。让它看到更多训练数据，往往能继续提高 performance。**

这不代表垃圾数据越多越好；Web-scale pre-training的关键恰恰是从大量候选数据中留下更值得训练的内容。

## 3. Step vs Epoch

一个 **training step**：

``` text
一个 batch
→ forward pass
→ loss
→ backpropagation
→ gradients
→ update weights
```

所以 **1 step ≈ 一次参数更新**。

一个 **epoch** 是整个 dataset 被完整看一遍。

如果数据太少，就需要多个 epochs：

``` text
Epoch 1 → 看一遍
Epoch 2 → 又看一遍
Epoch 3 → 又看一遍
```

反复学习相同 example 会增加 **overfitting** 风险。数据足够大时，有限compute budget 内可以让很多 step 都使用没见过的新数据。

## 4. 为什么最终要去 Web 找数据？

Curated data 很干净，但会用完。Internet 的优势是：

-   规模巨大
-   包含大量 general-domain human knowledge
-   持续增长和变化
-   大量内容可以通过网页访问

因此：**Web 是大规模语言模型最现实的数据来源之一。**

## 5. Web 数据的主要问题

### Noise

Internet 里既有好文章，也有 spam、广告、SEO 垃圾页面、fake news和其他低价值内容。

### Machine-generated / templated content

机器生成网页并不是 ChatGPT之后才出现。过去就有大量模板页面，只替换少数变量；生成式 AI普及后，这个问题更加突出。

### Duplicate content

同一篇文章可能被多个网站转载。如果不去重，模型会反复看到同样内容。

### Bias

Web 本身不是对全人类的均匀采样，可能存在：

-   recency bias
-   demographic bias
-   language bias

所以：**Web distribution ≠ real-world distribution。**

### Scraping restrictions

网站可能通过 paywalls、restrictive Terms of Service、Robots Exclusion Protocol 等方式限制自动抓取。

### Legality

课件指出，copyrighted text用于模型训练涉及独立的法律问题，并不存在一个简单统一的结论。

### Changing source

Internet 会变化：

``` text
Internet 2018 ≠ Internet 2024
```

新知识不断出现，同时一些过去开放的平台也可能逐渐限制 scraping。

## 6. robots.txt 大白话理解

可以把 `robots.txt` 想成网站给 crawler 放在门口的一块牌子：

``` text
这个 bot 不许进
这个目录不要抓
这里允许抓
```

课件的 Reddit 2019 示例说明，网站可以禁止某些 bots 或某些没有价值、可能破坏 crawler 的区域，同时允许其他内容被抓取。


2019
<img width="991" height="498" alt="image" src="https://github.com/user-attachments/assets/2432b130-2226-49c7-954d-85503362d4a9" />


2024
<img width="987" height="266" alt="image" src="https://github.com/user-attachments/assets/b6220222-7142-46cb-9d8c-3a8754c5d2a2" />


<img width="941" height="516" alt="image" src="https://github.com/user-attachments/assets/86343a4c-b0ca-47e5-a178-24b3bf566232" />


## 7. Web Data Pipeline

整条主线：

``` text
1. Content is posted to the Web
              ↓
2. Webcrawlers identify/download some content
              ↓
3. Data is filtered and cleaned
              ↓
       Pre-training corpus
```

### Stage 1 --- Content is posted

源头已经有偏差：

-   Recency bias
-   Demographic bias
-   Language bias

现代 Web 也不是简单 static HTML，还可能包含CSS、JavaScript、iframes、responsive design、secondary URLs、images、videos、tables 等。

而一个页面中的：

``` text
Navigation
Ads
Recommendations
Article
Footer
```

并不都是真正想训练的正文。

### Stage 2 --- Crawling

课件中的流程：

``` text
Seed URLs
   ↓ Explore
Target URLs
   ↓ Crawl
HTML Contents
   ↓ Scrape
Raw Texts
```

**Seed URL** 就是 crawler 的起点。Crawler 顺着 hyperlinks 不断发现新URL。

从 CS 角度可以把 Web 想成巨大 graph：

``` text
URL = node
hyperlink = edge
```

**Crawler vs Scraper：**

``` text
Crawler: URL → HTML
Scraper: HTML → useful raw text
```

Crawler 的主要难题：

1.  怎么高效获得大量 seed URLs？
2.  怎么选择 high-quality URLs、跳过 bad URLs？
3.  怎么保持 crawl up-to-date？

固定 compute budget 下还要权衡：

``` text
抓新网页
vs
重新抓可能已经更新的旧网页
```

### Stage 3 --- Filtering and Cleaning

目标主要有三个：

1.  删除 noisy / spammy / templated / fragmented text
2.  从巨大 candidate pool 中选择 higher-quality text
3.  避免不希望的 toxic / strongly biased content

核心原因：**Pre-training compute 有限，所以希望把昂贵的 GPU compute花在更值得学习的 token 上。**

## 8. C4 Rule-based Filtering 例子

课件用 T5 的 C4 dataset 举例，展示简单 heuristic rules 如何过滤 Common Crawl 文本。

示例包括：

-   从 Common Crawl 已提取的文本开始
-   只保留以 terminal punctuation 结尾的文本行
-   丢掉少于 5 个句子的页面
-   一行至少保留 3 个 words
-   删除包含 `javascript` 的行
-   删除符合某些明显低质量/不希望规则的页面，例如 `lorem ipsum`
-   做 three-sentence span level deduplication

直觉：

``` text
"This is a complete sentence."  → 更像自然语言
"Home Login Contact"            → 更像网页 UI 碎片
```

## 9. Deduplication 为什么重要？

Web 上同一内容可能被大量复制：

``` text
Original article
→ Site A
→ Site B
→ Site C
→ Site D
```

如果不 dedup：同一段内容 × 很多次，模型会错误地把"复制次数多"当成"应该学习很多次"。

因此： **Dedup 的目标是避免重复内容扭曲 training distribution。**

## 10. Filtering 真正困难在哪里？

### Granularity

应该在哪一层过滤？

``` text
word
sentence
paragraph
document
```

如果一个 document 只有一个 paragraph
很差，是删整篇还是只删那一段？没有永远正确的答案。

### 什么叫 High Quality？

Wikipedia 比较容易判断，但 Reddit discussion、personal blog、forum、informal language 是否 high-quality，就没有简单统一标准。

> **Quality definition 本身就是 dataset-design decision。**

### 什么叫 Non-toxic？

某个词出现，不代表整个 document就有害。它可能出现在新闻、学术、历史或引用语境中。因此简单 keyword filtering 可能产生 false positives。

### Filter 自己也可能有 Bias

如果 classifier 主要在 English上训练，再拿去过滤多语言数据，效果未必一致。即使都是English，也未必对所有 demographic groups 的表达方式同样公平。

``` text
Biased Web
    ↓
Possibly biased filter
    ↓
Final training distribution
```

### Dataset ablation 非常贵

要验证一个 filtering rule 是否真的更好，理想实验可能需要：

``` text
Dataset A → pretrain model
Dataset B → pretrain model
→ compare
```

但大模型 pre-training 极其昂贵，所以 dataset decisions 很难做彻底ablation。

## 11. 和上一节怎么连接？

上一节：

``` text
Clean Text
→ Tokenization
→ Token IDs
→ Batching
→ Training
```

这一节回答更前面的问题： **Clean Text 到底从哪来？**

完整链路：

``` text
Internet
→ Crawling
→ HTML
→ Scraping
→ Raw Text
→ Cleaning
→ Filtering
→ Deduplication
→ Clean Text
→ Tokenization
→ Batching
→ Pre-training
```

## 12. 面试版答案

### How do you build a web-scale pre-training dataset?

> A typical pipeline starts with seed URLs, crawls the web by following
> hyperlinks, downloads HTML, and scrapes raw text. Because web data is
> noisy and highly duplicated, the corpus then goes through cleaning,
> quality and safety filtering, and deduplication. Both crawling and
> filtering affect the final training distribution because the web
> already contains demographic, language, and recency biases, and the
> filters themselves can introduce additional bias.

### Crawler vs Scraper

> **Crawler finds and downloads pages; scraper extracts useful content from those pages.**

### Why is filtering difficult?

> Filtering is not just cleaning; it is dataset design. We need to
> define what high quality means, choose the filtering granularity,
> handle multilingual and demographic differences, and decide how
> aggressively to remove toxic or duplicated content.

## 13. Cheat Sheet

``` text
为什么需要 Web？
→ Curated high-quality data 会用完，LLM 非常 data-hungry。

Step？
→ 一个 batch → forward → loss → backward → weight update。

Epoch？
→ 整个 dataset 完整看一遍。

Web 的问题？
→ Noise / generated content / duplicates / bias /
   scraping restrictions / legality / changing source。

Web pipeline？
→ Content → Crawl → Scrape → Raw Text
  → Clean → Filter → Dedup → Tokenize → Batch → Train。

Crawler？
→ 找 URL、沿 hyperlink 扩展、下载网页。

Scraper？
→ 从 HTML 中抽取真正需要的 text。

为什么 Filtering？
→ Compute 有限，希望训练在更高价值的数据上。

C4 思路？
→ 用 rule-based heuristics 去掉碎片化、模板化、
   低质量或重复内容。

Filtering 最大难点？
→ High quality 没有完美定义；
   granularity、language、demographic bias 都会影响结果。

一句话总结：
→ At LLM scale, data quality and data selection are part of model design.
```

## 14. 面试 / Job Keywords

`LLM Pre-training`, `Pre-training Data Pipeline`, `Web Crawling`,
`Web Scraping`, `Data Curation`, `Data Filtering`, `Data Cleaning`,
`Deduplication`, `Dataset Quality`, `Dataset Bias`,
`Large-scale Data Processing`, `Web-scale Data Processing`,
`Distributed Data Processing`
