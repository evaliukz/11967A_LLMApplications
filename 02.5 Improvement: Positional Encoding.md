# Transformer Position Encoding：大白话笔记

<img width="886" height="514" alt="image" src="https://github.com/user-attachments/assets/eb28cf52-fbd8-4a80-949e-b2cb9b90d246" />


## 1. 为什么 Transformer 需要 Position Encoding？

Self-Attention 的核心是：

$$
score(q_j,k_i)=\frac{q_j\cdot k_i}{\sqrt{d_k}}
$$

它比较的是 token 之间“有多相关”，公式本身没有直接告诉模型 `i`、`j` 谁先谁后。

所以 vanilla self-attention 本身没有天然的 sequence-order prior。

大白话： **Token embedding 告诉模型“这个词是谁”，Position Encoding 告诉模型“这个词在哪里”。**

CNN 天生有 locality prior；RNN 按时间顺序递归，因此天然有 sequential prior。Transformer 可以让任意位置直接互相 attention，方便并行，但需要额外加入位置信息。

<img width="924" height="529" alt="image" src="https://github.com/user-attachments/assets/371412e8-27fb-4ddc-88cf-e67a1cdcf2ba" />

---

## 2. Absolute Position：告诉模型“我是第几个 token”

<img width="912" height="520" alt="image" src="https://github.com/user-attachments/assets/ca271053-f576-4198-b487-2b9441ea2c0a" />

例如：

```text
I      → position 0
love   → position 1
cats   → position 2
```

最经典的做法是：

$$
x'_i=x_i+p_i
$$

也就是：

```text
Token Embedding + Position Embedding
```

假设 `D=4`：

```text
love token embedding:
[0.50, -0.20, 0.10, 0.70]

position 1 embedding:
[0.05,  0.06, 0.07, 0.08]

相加：
[0.55, -0.14, 0.17, 0.78]
```

于是 Transformer 看到的是：

```text
“love 是什么”
+
“love 现在位于 position 1”
```

### 和之前 DecoderLM 的代码对应

```python
self.token_embeddings = nn.Embedding(n_vocab, n_embd)
self.position_embeddings = nn.Embedding(n_positions, n_embd)

token_embeddings = self.token_embeddings(input_ids)
positional_embeddings = self.position_embeddings(position_ids)

x = token_embeddings + positional_embeddings
```

这就是 **learned absolute position embedding**。

---

## 3. Absolute Position 的两种经典实现

### A. Sinusoidal Position Encoding

原始 Transformer 使用固定的 sin/cos：

$$
PE(pos,2i)=
\sin\left(\frac{pos}{10000^{2i/d}}\right)
$$

$$
PE(pos,2i+1)=
\cos\left(\frac{pos}{10000^{2i/d}}\right)
$$

不用死背公式。直觉是：

> **用很多速度不同的 sin/cos 波，给每个 position 制造一个独特的“位置指纹”。**

可以把 embedding 的不同 dimensions 想成很多只速度不同的钟：

```text
dimension 0 → 转得快
dimension 2 → 慢一点
dimension 4 → 更慢
...
```

同时读取这些“钟”，就可以描述 position。

### B. Learned Position Embedding

更直接：

```python
nn.Embedding(n_positions, n_embd)
```

即：

```text
position 0 → 一个可训练 vector
position 1 → 一个可训练 vector
position 2 → 一个可训练 vector
...
```

训练过程中模型自己学每个位置应该对应什么 vector。

---

## 4. Absolute Position 有什么局限？

比较：I took my dog Fido to the park 和：Yesterday after work I took my dog Fido to the park

第二句话前面多了几个 token，因此 `dog`、`park` 的 absolute positions 都改变了。

但是语言关系更关心的往往是：

```text
dog 和 park 相隔多少？
某个词在另一个词前面还是后面？
```

而不是：dog 恰好是不是 sequence 的第 137 个 token？

这引出 **Relative Position**。

---

## 5. Relative Position：告诉模型“你离我多远”

<img width="986" height="507" alt="image" src="https://github.com/user-attachments/assets/be8fb6ff-191d-4396-9ac1-1298ae9b611f" />

普通 Attention：

$$
score(q_j,k_i)=
\frac{q_j\cdot k_i}{\sqrt{d_k}}
$$

加入 relative position 后，可以写成：

$$
score(q_j,k_i)=g(q_j,k_i,i-j)
$$

最重要的是：

$$
i-j
$$

它表示 Key 相对 Query 的距离和方向。

一种直观实现是 relative position bias：

$$
score(q_j,k_i) =
\frac{q_j\cdot k_i}{\sqrt{d_k}}
+
b_{i-j}
$$

其中 $b_{i-j}$ 可以是可训练参数。

例如：

```text
semantic score = 1.2
relative-position bias = +0.5

final score = 1.7
```

于是模型不仅考虑：

```text
“这两个 token 语义相关吗？”
```

还可以考虑：

```text
“它们的位置关系合适吗？”
```

### 一句话区别

```text
Absolute Position:“我是 position 137。”

Relative Position:“你在我前面 3 个 token。”
```

---

# 6. RoPE：Rotary Position Embedding

RoPE 的核心只有一句： **根据 token 的 position，把 Q 和 K 向量旋转不同的角度。**

<img width="959" height="432" alt="image" src="https://github.com/user-attachments/assets/c50949a8-2ed6-4db5-a79a-7ce5ed598178" />

<img width="984" height="517" alt="image" src="https://github.com/user-attachments/assets/e62fc966-4fc5-460d-955c-b8869f50afa3" />

<img width="868" height="447" alt="image" src="https://github.com/user-attachments/assets/0f41246b-220c-49ea-81a9-1bb0e5ef12bc" />


传统 additive position encoding：

$$
x'=x+p_{pos}
$$

RoPE 不主要靠把 position vector 加到 token embedding 上，而是：

```text
hidden states
↓
生成 Q / K / V
↓
根据 position 旋转 Q 和 K
↓
计算 rotated Q · rotated K
↓
Attention
```

---

## 7. 为什么“旋转”可以表示 position？

先看二维空间。

假设：

```text
q = [1, 0]
```

它原来指向右边。

如果：

```text
position = 1
θ = 30°
```

就把 q 旋转：

```text
1 × 30° = 30°
```

另一个 K 在 position 3，则旋转：

```text
3 × 30° = 90°
```

两者之间因为 position 引入的角度差：

$$
90^\circ-30^\circ=60^\circ
$$

也就是：

$$
(3-1)\theta
$$

于是虽然各自使用的是 absolute position 1 和 3，最后 Q/K interaction 中自然出现了：

$$
i-j
$$

即 **relative position**。

---

## 8. 二维旋转公式

二维向量：

$$
x=
\begin{bmatrix}
x_1\\
x_2
\end{bmatrix}
$$

旋转角度 $\phi$ 的矩阵：

$$
R(\phi)=
\begin{bmatrix}
\cos\phi & -\sin\phi\\
\sin\phi & \cos\phi
\end{bmatrix}
$$

所以：

$$
x'=R(\phi)x
$$

RoPE 让：

$$
\phi=position\times\theta
$$

---

## 9. 为什么 rotation 特别适合 Attention？

Attention 使用 dot product：

$$
q\cdot k
$$

几何上：

$$
q\cdot k=
||q||||k||\cos(\alpha)
$$

所以 dot product 与两个向量之间的夹角直接相关。

RoPE：

```text
position
↓
改变 Q/K 的旋转角度
↓
改变它们的几何关系
↓
影响 Q · K
↓
影响 Attention score
```

因此 positional information 被直接编码进 Attention。

---

## 10. RoPE 最关键的数学性质

Query 在 position $j$：

$$
Q'_j=R(j\theta)Q_j
$$

Key 在 position $i$：

$$
K'_i=R(i\theta)K_i
$$

dot product：

$$
(Q'_j)^TK'_i
$$

代入：

$$
Q_j^TR(j\theta)^TR(i\theta)K_i
$$

旋转矩阵满足：

$$
R(j\theta)^T=R(-j\theta)
$$

因此：

$$
R(-j\theta)R(i\theta)=
R((i-j)\theta)
$$

最终：

$$
(Q'_j)^TK'_i=
Q_j^TR((i-j)\theta)K_i
$$

### 最值得理解的地方

最后的位置关系只通过：

$$
i-j
$$

出现。

所以： **RoPE 使用 absolute position 决定每个 Q/K 怎么旋转，但两个 token 做 Attention 时，自然产生 relative-position structure。**

---

## 11. 一个最直观的例子

比较：

```text
Query position = 100
Key position   = 103
```

和：

```text
Query position = 500
Key position   = 503
```

absolute positions 完全不同。

但是：

$$
103-100=3
$$

$$
503-500=3
$$

两组 token 有相同的 relative distance。

这就是 RoPE 想利用的结构。

---

## 12. “距离远就 Attention 小”并不是严格规则

课件里的旋转图可以帮助建立直觉，但不要背成：

```text
token 越远 → attention 一定越小
```

这是不准确的。

真正的 Attention 仍取决于：

```text
Q/K 的内容
+ position-induced rotation
+ 不同维度的不同 frequencies
+ 模型训练学到的 representations
```

RoPE 的关键是： **让 relative position 影响 Q/K dot product。**

而不是简单制造固定的“距离惩罚”。

---

## 13. 高维 Q/K 怎么旋转？

<img width="954" height="434" alt="image" src="https://github.com/user-attachments/assets/267d4a96-a91e-470a-a833-d951f54dcbef" />

真实模型的 head dimension 可能是：

```text
64
128
...
```

RoPE 的方法是：

# 两个 dimensions 一组

例如：

```text
Q =
[q0,q1,q2,q3,q4,q5,q6,q7]
```

拆成：

```text
(q0,q1)
(q2,q3)
(q4,q5)
(q6,q7)
```

每一对都当成一个二维向量做 rotation。

并且不同 pair 使用不同 frequency：

```text
(q0,q1) → θ1
(q2,q3) → θ2
(q4,q5) → θ3
(q6,q7) → θ4
```

可以继续用“很多只速度不同的钟”来理解。

---

## 14. RoPE 和 Sinusoidal Position Encoding 有什么关系？

Sinusoidal PE 中有：

```text
sin(position × frequency)
cos(position × frequency)
```

RoPE 的 rotation matrix 也有：

$$
\begin{bmatrix}
\cos(position\theta) & -\sin(position\theta)\\
\sin(position\theta) & \cos(position\theta)
\end{bmatrix}
$$

所以两者数学思想有亲缘关系。

但使用方式不同：

```text
Sinusoidal PE
→ position information 加到 input embedding

RoPE
→ 用 sin/cos rotation 作用在 Q/K
```

---

## 15. RoPE 在 Attention 的哪个位置？

你之前写过：

```python
q = self.q_proj(x)
k = self.k_proj(x)
v = self.v_proj(x)
```

如果加入 RoPE，概念上：

```python
q = self.q_proj(x)
k = self.k_proj(x)
v = self.v_proj(x)

q = apply_rope(q, position_ids)
k = apply_rope(k, position_ids)

scores = q @ k.transpose(-2, -1)
```

所以位置是：

```text
生成 Q/K
   ↓
★ RoPE ★
   ↓
QK^T
   ↓
Scale
   ↓
Mask
   ↓
Softmax
   ↓
× V
```

### 为什么主要作用在 Q/K？

因为：

```text
Q 和 K
→ 决定 Attention score
→ 决定“应该看谁”

V
→ 提供真正被取回的信息
```

所以 RoPE 通常作用于 Q/K，使 position information 直接影响“看谁”。

---

## 16. 用 `I love cats` 串起来

```text
I     → position 0
love  → position 1
cats  → position 2
```

假设某个 head 中：

```text
Q_love
K_cats
```

RoPE：

```text
Q_love → rotate by 1θ
K_cats → rotate by 2θ
```

然后：

$$
Q'_{love}\cdot K'_{cats}
$$

位置产生的相对 rotation 与：

$$
(2-1)\theta
$$

有关。

因此 Attention interaction 可以知道：

```text
cats 相对 love 在后面 1 个位置
```

---

# 17. 三种方法彻底区分

| 方法 | Position 信息放哪里 | 大白话 |
|---|---|---|
| Sinusoidal PE | input embedding | 用固定 sin/cos 告诉模型“我在哪” |
| Learned Absolute PE | input embedding | 每个 position 学一个 vector |
| Relative Position Bias | attention score | 直接根据 $i-j$ 调整 score |
| RoPE | Q/K | 根据 position 旋转 Q/K，让 dot product 自然包含相对位置 |

最简单的记忆：

```text
Absolute:
“我在哪里？”

Relative:
“你离我多远？”

RoPE:
“我根据各自位置旋转 Q/K，
最后让相对距离自然出现在 Q·K 里。”
```

---

# 18. RoPE 不等于无限 Context

<img width="899" height="436" alt="image" src="https://github.com/user-attachments/assets/982ba4b3-1e1c-4dae-a25e-0f5a374b6abe" />

RoPE 有很漂亮的 relative-position structure，但不能直接推出：

```text
模型训练时 context = 4K
↓
不做任何修改
↓
就能完美处理 1M context
```

超出训练的位置范围后仍可能有 extrapolation 问题。

因此后来还有很多：

```text
RoPE scaling
position interpolation
frequency scaling
```

等长上下文技术。

---

# 19. LLaMA-style architecture：把前几节串起来

<img width="777" height="518" alt="image" src="https://github.com/user-attachments/assets/08e3d387-4bd7-4046-8a51-d5b90a01438f" />

这几节课其实是在告诉你：

> Transformer 的大骨架没有被推翻，但里面的零件不断升级。

典型 modern decoder-only LLM architecture choices：

```text
Feed Forward
→ gated FFN，例如 SwiGLU

Attention
→ MHA / GQA 等

Normalization
→ Pre-Norm + RMSNorm 很常见

Position
→ RoPE 很常见
```

可以整体想成：

```text
Token Embeddings
       ↓
┌──────────────────────────────┐
│ RMSNorm                      │
│    ↓                         │
│ Q / K / V Projection         │
│    ↓                         │
│ RoPE on Q and K              │
│    ↓                         │
│ Attention / GQA              │
│    ↓                         │
│ Residual Add                 │
│                              │
│ RMSNorm                      │
│    ↓                         │
│ FFN / SwiGLU                 │
│    ↓                         │
│ Residual Add                 │
└──────────────────────────────┘
          × N
       ↓
Final Norm
       ↓
Vocabulary Logits
       ↓
Next Token
```

---

# 20. 从经典 Transformer 到现代 LLM

| Component | 经典/较早设计 | 现代 LLM 常见选择 |
|---|---|---|
| Position | sinusoidal / learned absolute | RoPE |
| Norm placement | original Transformer: Post-LN | Pre-Norm common |
| Norm | LayerNorm | RMSNorm common |
| FFN | ReLU/GELU MLP | gated FFN / SwiGLU common |
| Attention | MHA | GQA/MQA used by many models |
| 主要目标 | Transformer 能工作 | 更稳定、更高效、更容易 scale |

不是所有现代模型都完全一样，这张表是 architecture trend。

---

# 21. 面试答案

### Why does Transformer need positional encoding?

> **Self-attention by itself does not encode sequence order. Positional encoding injects position information so the model can distinguish tokens at different positions and reason about their positional relationships.**

### Absolute vs Relative Position Encoding?

> **Absolute positional encoding tells the model where each token is in the sequence, while relative positional encoding represents the distance or offset between pairs of tokens.**

### What is RoPE?

> **RoPE rotates the query and key vectors by position-dependent angles. Because their dot product depends on the difference between those rotations, the attention interaction naturally captures relative positional information.**

更短的面试版本：

> **RoPE rotates Q and K according to their positions so that their dot product naturally contains relative position information.**

### Why apply RoPE to Q and K?

> **Q and K determine the attention scores. Rotating them injects positional information directly into the query-key dot product, which controls which tokens attend to each other.**

---

# 22. 考试前 30 秒版本

```text
Transformer self-attention 本身没有天然顺序。

Absolute PE:
“我是第几个 token。”

x' = x + position_embedding


Relative PE:
“你离我几个 token。”

score = content_score + relative_position_information


RoPE:
根据 position 旋转 Q 和 K。

Q at j → rotate by jθ
K at i → rotate by iθ

做 dot product 后，位置关系表现为：

(i - j)θ

所以：

RoPE 用 absolute positions 做 rotation，
但让 Attention interaction 自然具有
relative-position structure。
```

# 最终 Mental Model

> **Token Embedding 解决“你是谁”；Position Encoding 解决“你在哪里”。Absolute Position 关注“我在第几位”，Relative Position 关注“你离我多远”，而 RoPE 通过旋转 Q/K，把这种相对位置关系直接融进 Attention 的 dot product。**



