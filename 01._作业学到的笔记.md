
# 1. MultiHeadAttention，实现Transformer的self attention，让模型关注输入中的不同部分

流程 X → Q/K/V → split heads → QKᵀ/√d_k → softmax → weighted V → concat heads → output projection


# 2. Dimension symbols:
<pre>
    B - batch size
    S - sequence length
    D - hidden dimension (n_embd)
    H - number of attention heads (n_head)
    HD - hidden dimension of a single attention head (d // n_head)
    V - size of the vocabulary
</pre>

这几个变量其实就是你做 Multi-Head Transformer 时最重要的一套 shape vocabulary。

一个 Transformer 输入通常可以看成一个三维 tensor：[B, S, D] 也就是：batch 里有 B 个句子，每个句子有 S 个 token，每个 token 用 D 维向量表示。

## B = batch size

表示一次 forward pass 里，同时处理多少条 sequence。

例如你有 4 个句子一起送进模型：

"I love cats", 
"She likes coffee", 
"Transformers are powerful", 
"Hello world"

那么：B = 4

如果每条都 padding / truncation 到长度 8，那么输入 tensor 可能是 x.shape = [4, 8, D]

其中第一维就是 B=4。

你可以理解成：B 只是“同时并行处理多少个样本”。

它不会改变 Transformer 的核心数学逻辑，只是为了 training efficiency。

## S = sequence length

表示：一条 sequence 里有多少个 token position。

例如："I love machine learning"

假设 tokenize 后：["I", "love", "machine", "learning"]， 那么：S = 4

当然现实里通常会有：padding，BOS，EOS，subword tokenization，所以实际 S 是 token 数，不一定是 word 数。

例如："unbelievable"，可能被切成多个 subword token。

为什么 S 很重要？Self-Attention 要让每个 token 和所有 token 比较。

因此 attention score matrix 大小是：S × S

如果：S = 128 那么每个 head 的 attention matrix：128 × 128

如果：S = 4096 则：4096 × 4096 所以 vanilla attention 的复杂度大约是：O(S²)， 这就是长 context 很贵的根本原因之一。

## D = hidden dimension = n_embd

这是最核心的维度之一。它表示：每个 token 在 Transformer 内部用多少维向量表示。

例如：D = 768，那么每个 token 是一个 768 维向量：token → [0.12, -0.43, ..., 0.88] 长度是 768。

所以整个 batch 的 hidden states：x.shape = [B, S, D] 例如：B = 32，S = 128，D = 768 那么：x.shape = [32, 128, 768]

D 到底代表什么？可以理解成：每个 token 的“representation capacity”。

这个向量里可能编码：syntax，semantics，position-related context，entity relationships，other learned features 但不要理解成“某一维固定代表某个概念”。

D 在 Transformer 里通常贯穿始终，例如：Embedding output → [B, S, D]， Attention output → [B, S, D]  FFN output → [B, S, D]

Residual connection 之所以可以直接：x + attention_output 就是因为 shape 必须一样：[B, S, D]

## H = number of attention heads = n_head

表示：一个 Multi-Head Attention 里有多少个 attention head。

例如：H = 12意味着同时运行 12 个不同的 attention mechanism。每个 head 可以学习不同的 relationship pattern。

例如某些 head 可能偏向：local context，subject-verb relation，coreference，punctuation pattern，long-range dependencies

不过这只是直觉，不要认为每个 head 都有人类可解释语义。

## HD = hidden dimension per head

通常：HD = D // H 因为整个 hidden dimension D 被分给 H 个 heads。

例如：D = 768 H = 12 那么：HD = 768 // 12 = 64  所以：12 heads × 64 dimensions = 768

也就是：H × HD = D 这是一个特别重要的关系。

为什么要拆成 Heads？

输入原本：[B, S, D] 比如：[B, S, 768]

Multi-Head Attention 会 reshape 成：[B, S, H, HD] 例如：[B, S, 12, 64]

通常接着 transpose 成： [B, H, S, HD]

为什么这样做？因为这样每个 head 就可以独立做 attention。

你可以把 D 想成被切成 H 份，比如：D = 8，H = 2，HD = 4

一个 token 的 hidden vector：[a b c d e f g h] 可以 conceptualize 成：Head 1: [a b c d]  Head 2: [e f g h]

实际代码里不是简单机械切 input，而通常先经过 learned linear projections：

Q = x @ Wq
K = x @ Wk
V = x @ Wv

然后再 reshape 成多个 heads。


